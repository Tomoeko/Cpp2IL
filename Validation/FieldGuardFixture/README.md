# Instance-field null guard control

Build this synthetic fixture with Unity 2021.3.35f1 Windows x64 Release IL2CPP. Its methods read and write signed `int` and `long` fields, read and clear a Boolean field, and clear a signed `int` field through either a direct or nested reference. The expanded control also writes `true` and `false` through a reference field on unsealed classes. A wrapper selects a result from a direct Boolean-return call; its neighboring-field observation checks that the call and selection do not alter adjacent storage. The independent harness covers null and populated receivers, signed integer boundaries, both Boolean values, null field owners, a null instance reader, and unchanged neighboring fields. All nineteen selected methods passed strict player-only recovery, typed IL verification, declaration comparison, exact Unity compilation, Release IL2CPP rebuilding, and editor/player behavior validation over 54 observations. The wrapper's native `test al,al` has a ZF-only register conditional move after two flag-preserving immediate moves.
