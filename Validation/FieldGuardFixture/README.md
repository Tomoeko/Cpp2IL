# Instance-field null guard control

Build this synthetic fixture with Unity 2021.3.35f1 Windows x64 Release IL2CPP. Its methods read and write signed `int` and `long` fields and clear a Boolean field on sealed reference classes. The independent harness covers null and populated receivers, signed integer boundaries, and both initial Boolean values. The byte-sized zero store is accepted only when the native null branch, nonreturning runtime helper, unique method binding, unchanged Boolean field and caller unwind region match the closed proof. All eight selected methods have passed player-only recovery, exact Unity compilation and native behavior validation.
