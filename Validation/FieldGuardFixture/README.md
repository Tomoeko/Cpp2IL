# Instance-field null guard control

Build this synthetic fixture with Unity 2021.3.35f1 Windows x64 Release IL2CPP. Its methods read and write signed `int` and `long` fields on sealed reference classes. The independent harness covers null and populated receivers, including signed integer boundaries. Both bounded scopes have passed player-only recovery, exact Unity compilation and native behavior validation.
