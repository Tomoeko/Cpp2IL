# Instance-field null guard control

Build this synthetic fixture with Unity 2021.3.35f1 Windows x64 Release IL2CPP. Its methods read and write signed `int` and `long` fields, clear a Boolean field, and clear a signed `int` field through a nested reference. The independent harness covers null and populated receivers, signed integer boundaries, both initial Boolean values, and null inner and outer references. The zero stores are accepted only when the native null branch, nonreturning runtime helper, unique method binding, unchanged field layout and caller unwind region match a closed proof. All ten selected methods have passed player-only recovery, exact Unity compilation and native behavior validation.
