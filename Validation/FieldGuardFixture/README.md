# Instance-field null guard control

Build this synthetic fixture with Unity 2021.3.35f1 Windows x64 Release IL2CPP. Its methods read and write signed `int` and `long` fields, read and clear a Boolean field, and clear a signed `int` field through either a direct or nested reference. The expanded control also writes `true` and `false` through a reference field on unsealed classes. The independent harness covers null and populated receivers, signed integer boundaries, both Boolean values, null field owners, a null instance reader, and unchanged neighboring fields. The original fourteen selected methods previously passed player-only recovery, exact Unity compilation and native behavior validation. The two new literal stores and their constructors require a fresh exact-target round trip before an acceptance claim.
