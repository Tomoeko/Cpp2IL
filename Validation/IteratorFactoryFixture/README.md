# Iterator factory fixture

An instance iterator yields one object field. Its factory must create a fresh enumerator and capture the owner, while the generated iterator type supplies the enumeration methods. The player-only audit must inventory every retained method in this assembly before any recovery claim. A proof of the factory alone cannot establish strict whole-assembly recovery if the generated methods remain unresolved.

The independent harness checks fresh enumerators, deferred field reads, null values, enumeration completion, disposal and `Reset` behavior. The exact Windows Unity 2021.3.35f1 baseline passed its original editor and Release IL2CPP player checks. The current player-only run recovers all eight selected method bodies with valid typed IL. The generated `Reset` has a complete 70-byte terminal throw function; the metadata-derived method span continues into a later native function. The throw proof accepts only the unwind-bounded function after authenticating the MethodDef, exception construction and raise path.

The indexed `System.Type` attribute now passes the strict in-memory emission gate, so fresh player-only recovery emits all eight selected methods and generates Unity source with no source diagnostics. `DECL001` and `DECL004` retain player-only uncertainty about return rows and the original serialized Type-name qualification. The historical six-of-eight and seven-of-eight checkpoints remain useful recovery measurements; their diagnostic `Reset` fallback was not recovered behavior.

The regenerated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds a Windows x64 Release IL2CPP player. Fresh original and recovered editor/player runs each pass 16 observations for fresh enumerators, deferred owner reads, completion, disposal and `Reset`, with matching observation arrays. Recovered managed and rebuilt stripped declarations each match a fresh original stripped managed oracle in the independent comparer. Its decoded `IteratorStateMachineAttribute` serialized Type name matches across original stripped, original unstripped, recovered managed and rebuilt stripped assemblies. This exact fixture comparison does not make the lost SerString qualification available from player metadata generally. The finite probe does not establish all exceptional paths or general iterator recovery.
