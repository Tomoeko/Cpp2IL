# Unity 2021.3.35f1 recovery roadmap

## Goal and scope

Produce accurate, readable C# from **Unity 2021.3.35f1 Windows x64 Release IL2CPP** player inputs. Generated source must compile in the supplied exact editor, and recovered behavior must be verified against controlled source/build pairs. The target is **1:1 managed-structure and behavioral fidelity**. Unresolved or unavailable information remains a gap toward that target. Reproduction of erased source text or byte-identical native binaries is outside this accuracy definition.

The user has confirmed this roadmap and `AGENTS.md` and authorized full implementation. Milestones 0 and 1 have passed their initial synthetic acceptance scope. Later milestones remain active; this is not a whole-project recovery certification.

Windows x64 is the initial platform. Other Unity versions, architectures, obfuscation variants, and full asset/scene reconstruction are outside the first delivery. Existing cross-platform support should remain usable where practical. Assets are not prerequisites for a code-only recovery milestone; serialization and scene compatibility require separate evidence when claimed.

## Inspected starting point

Baseline inspection: repository revision `b5ad444` on `development`.

| Area | Observed implementation | Consequence |
| --- | --- | --- |
| Parsing | `LibCpp2IL/Metadata/Il2CppMetadata.cs` includes Unity 2021.3 metadata handling | Parsing support does not establish exact-version code recovery |
| Analysis | `MethodAnalysisContext.Analyze()` lifts instructions, constructs control flow, and runs stack/SSA, metadata and recovery passes | Improve and validate this pipeline before replacing it wholesale |
| Managed output | `AsmResolverDllOutputFormatIlRecovery` emits IL, skips selected framework/engine bodies, and falls back to stubs or throwing bodies | Current success counts do not prove complete recovered behavior |
| IL validity | `Cpp2IL.Core/IlGenerator.cs` disables automatic max-stack computation and contains unsupported-instruction diagnostics and missing-value default substitutions | Validate stack/type correctness and report degradation explicitly |
| C# output | `DiffableCsOutputFormat` writes declaration snapshots with empty bodies and file-scoped namespaces | This is not a Unity-compatible implementation or project exporter |
| Tests | Existing parser/core tests cover other Unity fixture versions; no dedicated 2021.3.35f1 validation path was found in the inspected loaders | Add a small exact-target fixture and an end-to-end acceptance path |
| Build setup | Current projects/CI use .NET 10; `global.json` selects the test runner without pinning an SDK | Establish the actual local baseline; do not rely on stale README versions |
| CI | The existing test/publish job is restricted to the upstream repository | Fork build status alone must not be presented as test evidence |

The older local recovery tool supplies investigation ideas only. Its outputs and plans are not evidence that this fork can compile or reproduce behavior. The baseline observations above describe the inspected starting revision; implementation evidence is recorded below.

## Acceptance model

Keep these gates distinct in reports. Scope each result to an explicit fixture/input set and settings.

| Gate | Required evidence |
| --- | --- |
| Input/profile validity | Exact editor version, PE/x64 input, metadata/registration consistency, and recorded known/unknown build settings |
| Declaration fidelity | Assembly/type/member signatures, generic constraints, attributes, constants and layout compared with a known oracle |
| Managed IL validity | Valid instructions, references, control-flow joins, stack/type behavior and exception regions for methods represented as recovered IL |
| Unity compilation | Fresh generated C# imports and compiles in Unity 2021.3.35f1 with the intended platform symbols, references and API profile |
| Native rebuild | Fresh Windows x64 IL2CPP player build using C++ compiler configuration `Release`, with a successful build report and artifacts |
| Behavioral fidelity | Original and recovered builds agree on tested return values, mutations, side effects, dispatch, initialization and exceptions |
| Unity integration | When in scope, component identity, serialized fields and script/asset bindings are verified independently |

Unity's [IL2CPP build pipeline](https://docs.unity3d.com/2021.3/Documentation/Manual/IL2CPP.html) includes managed compilation, stripping, C++ conversion and native compilation. A managed compile is therefore only one acceptance stage. The [C++ compiler configuration](https://docs.unity3d.com/2021.3/Documentation/ScriptReference/Il2CppCompilerConfiguration.html) must be `Release`; a .NET `-c Release` build, an unchecked Development Build option, or a `Master` native build is not equivalent evidence.

Report total input methods and types, methods with native bodies, emitted bodies, partial/unsupported bodies, fallbacks, failures, and exclusions with reasons. Record compilation results and behaviorally verified method counts separately. Generic/shared native addresses must not collapse distinct managed methods in the denominator. Never improve a metric by silently dropping hard cases or counting stubs as recovered.

## Milestone 0 — Reproducible baseline and truthful reporting

Status: complete for the initial baseline. The complete solution restores and builds with .NET SDK 10.0.107; all 76 pre-change core tests passed. An earlier solution `dotnet test --solution Cpp2IL.slnx -c Release --no-build --no-restore` run discovered 1,562 tests: 1,481 passed, none failed and 81 optional fixture tests skipped, with the exact field-array, literal-concatenation, iterator, direct-constructor iterator, static-setter, class-cast, generic-dispatch, guarded-sink and folded-constructor player inputs supplied. Six focused cast/literal/type-token/throw proof tests pass with their exact synthetic player inputs supplied. The focused Boolean-getter suite passes 18/18 when its earlier exact synthetic player input is supplied. Focused native reference-store, byte-threshold and integer-array tests pass 10/10, 15/15 and 12/12 with their exact fixture inputs supplied; the historical field-array and prior scalar-array focused cases passed 19/19, and the direct reference-array read cases pass 3/3. A later bounded proof restores strict recovery for the field-array constructor as described below. Other native loop-call, scalar-truncation, field-access and exception-region integration checks are available when their fixture inputs are supplied. The current net10.0 CLI builds without errors; a package-generating solution build reports four existing prerelease Disarm warnings. Eighty-three public Python harness checks and the declaration comparer's mutation checks pass without Unity. The offline parser suite passes three local cases and explicitly skips five optional external samples. One earlier unfiltered Microsoft Testing Platform invocation exited with zero discoveries; immediate repeats passed, so that result is retained as a transient failure rather than test evidence. Private baseline logs are retained locally.

A later local solution build with SDK 10.0.107 passed with the four existing prerelease Disarm packaging warnings. The full offline solution test command discovered 1,571 tests: 1,472 passed, none failed and 99 optional-input tests skipped; the new instance-reference-setter native proof instead passed all nine focused checks with its exact synthetic player input supplied. The 83 public Python harness checks and the declaration comparer's independent mutation checks also pass. No external parser downloads were enabled. A later full Release solution build passed with zero reported warnings or errors. With the unused-reference nested-store synthetic player input supplied, the offline solution test run discovered 1,572 tests: 1,473 passed, none failed and 99 optional-input tests skipped. The new proof test and the retained zero-parameter nested-store exact-player test both pass; 83 public Python harness tests also pass. A subsequent full Release build with the read/write-property proof passed with four existing Disarm `NU5104` package warnings and zero errors. With both new exact synthetic player inputs supplied, the solution run discovered 1,573 tests: 1,474 passed, none failed and 99 optional-input tests skipped; the 83 public Python harness checks also pass.

The offline core suite now reuses a read-only public player model within three test fixtures while each case constructs its own synthetic graph or assembly. Two full runs passed the same 1,277 tests with 28 optional skips in 25.73–25.88 seconds, compared with 203.87 seconds before this test-only change on the same host. The standalone declaration-comparison mutation check now builds independent synthetic projects concurrently and passed in 5.0–6.7 seconds across three runs, compared with about 10.7 seconds serially. These harness improvements do not shorten or replace a fresh Unity native-build acceptance gate.

Report registration now assigns sequential method ordinals without reading `ConcurrentDictionary.Count` for every method. A local synthetic 91,198-entry benchmark measured 0.79–0.80 seconds for repeated count reads and 0.005–0.006 seconds for sequential ordinals across warm trials. Both full private-input audits preserved every ordered method identity after the change; end-to-end audit times remain dominated by recovery and are not claimed to improve by the benchmark ratio.

An exact-target negative source-export control with no configured reference assemblies now exits with status 1 while retaining its `SOURCE001` failure report. Unexpected CLI errors are logged as failed commands instead of terminating through an unhandled exception; this does not turn a failed export into a compilation result.

Completed local Unity runs had accumulated about 84.5 GiB under ignored `Files/`. A guarded cleanup removed 391 reproducible project/player/input trees, about 71.7 GiB, while retaining receipts, logs and recovered source and excluding active runs. A dry-run-first pruning command now requires a terminal receipt and a 24-hour default age, records its own private manifest and preserves small editor report witnesses. Pruned baselines must be rebuilt before reuse.

Cleanup can now be scoped to named validation runs while retaining player inputs. A Boolean control exposed a lifecycle error when an original managed declaration oracle was pruned during an otherwise successful recovered build; that first run is a failed validation receipt. The round-trip runner now snapshots and hashes both original managed oracles after player-only recovery and typed IL checks, verifies them at both declaration comparisons, and keeps them outside recovery inputs and reference directories. A fresh repeat passes both comparisons. The completed runs' generated project/player trees were then pruned with a scoped dry run and private manifest; receipts, logs, recovered source, oracle snapshots and fixture player input remain.

The completed eleven-method Boolean original and recovered runs were reviewed and pruned with the same dry-run-first workflow. Four generated Unity project/player trees occupying about 1.13 GiB were removed; the passing receipts, recovered source, managed-oracle snapshots and player inputs remain under ignored `Files/`.

An isolated broad original build attempt generated about 32 GiB of project and player copies. Its terminal failed receipt and serialized Unity report were reviewed before a dry-run and scoped removal of both generated trees. Only the logs, receipts, build report and three minimal diagnostic player-input files remain under ignored `Files/`.

The cleanup command now also accepts an explicit `--runs --only RUN_NAME` scope for completed generated project/player trees under ignored `Files/runs`. It keeps the dry-run default, receipt-age and ignored-path gates, report witnesses, manifests and optional player inputs. Synthetic CLI regressions cover the required selection, retained artifacts and refusal when the ignored-path check fails; no broad `Files/runs` prune was performed.

After the instance-reference-setter round trip and its receipts were reviewed, scoped dry runs preceded removal of five generated project/player/input trees totaling about 1.18 GiB. The original fixture's player input, recovered source, receipts, logs and report witnesses remain under ignored `Files/`. Its pruned original project requires a fresh build before a later `--baseline-run` reuse.

After the unused-reference nested-store and read/write reference-property round trips, reviewed scoped dry runs preceded removal of four generated project/player trees per fixture, about 1.15 GiB and 1.16 GiB respectively. Their original player inputs, recovered source, receipts, logs, managed-oracle snapshots and small report witnesses remain under ignored `Files/`; a pruned original project requires a fresh build before `--baseline-run` reuse.

Implemented evidence:

- Recovery reports retain every input method, distinguish exclusions and failures, and reject detected incomplete recovery in strict mode. Labels and stack depth are checked separately from typed IL verification and behavior. Regression cases reject missing values and unsupported operations rather than inventing defaults.
- A synthetic assembly with three authored methods and one implicit constructor builds in the supplied exact Windows editor. Its original Windows x64 IL2CPP Release player passes 81 independent integer boundary/overflow input pairs. Actual settings are nondevelopment, `NET_Unity_4_8`, Low stripping, OptimizeSpeed, MSVC 14.29.30133 and Windows SDK 10.0.19041.0. This is original-fixture evidence, not recovered-source evidence.
- The fresh-project harness records editor compilation, editor behavior, native build and player behavior independently. It refuses empty behavioral reports, stale output and timed-out processes even if they later exit successfully. Private player loading also succeeds; metadata-only stubs remain explicitly unverified behavior.
- Fork CI now runs the offline suites, and publishing stays restricted to the original repository. No remote workflow or push was performed.
- The synthetic player baseline contains 11,904 input methods. Default DLL and diffable-source output produce 37 metadata/stub assemblies and 1,775 declaration files. These counts are not recovered behaviors. The initial recovery report counts seven emitted bodies, 23 unresolved and 11,874 explicit exclusions across the player; the verified selection is only the four-method synthetic fixture assembly.

- Confirm the local .NET SDK, package restore, repository build, and relevant existing tests. Distinguish infrastructure failures from code failures. Record a working SDK baseline and resolve stale build guidance when justified.
- Inventory the supplied editor, Windows IL2CPP support, Wine environment and native toolchain. Record versions and usable host/target combinations locally; do not infer the original player's compiler flags from a directory name.
- Define a target profile for 2021.3.35f1, PE x64 and Release. Record API compatibility level, stripping, code-generation options, symbols, compiler/SDK versions and other known settings. Label unknown player settings instead of silently assuming them.
- Add a small, synthetic, redistributable source fixture built with the exact profile. Preserve original source, managed output and generated native material as separate validation oracles; run recovery from an isolated copy of player inputs.
- Baseline current DLL and diffable-source output, then add explicit method dispositions and reasons. Cover empty analysis, method-size limits, unsupported instructions, default substitutions and caught failures.
- Keep public fixtures/configuration neutral and private raw runs under `Files/`. Separate ordinary public tests from optional local integration runs; unavailable licensed tooling must be reported as skipped/unverified.

Exit evidence: reproducible commands and settings; an honest feature/error inventory with fixed denominators; no existing recovery limitation disguised as successful behavior.

## Milestone 1 — One complete recovery path

Status: complete for the initial four-method slice. The `cs_unity` exporter uses recovered CIL and pinned ICSharpCode.Decompiler 9.1.0.7988, explicit target references and selected application assemblies. All four methods recovered from isolated native player inputs pass typed IL verification. Their freshly generated C# compiles in Windows Unity 2021.3.35f1, rebuilds as Windows x64 Release IL2CPP, and passes all 81 independent input pairs in both editor and native player. No generated source was manually repaired. The constructor is exercised as a required dependency. This establishes finite tested behavior, not whole-program equivalence.

A separate negative strict run selects the more complex validation-driver assembly: four of its six methods remain unresolved, and strict mode rejects source output while retaining the full disposition report. The original managed DLL was used separately to validate the decompiler component; it was not supplied to the successful player-only recovery command.

The following separate fixture scopes have complete native roundtrip receipts. Each passed player-only recovery, typed IL verification, declaration comparisons against the original managed oracle, exact-editor source compilation, a Windows x64 Release IL2CPP rebuild, and the listed original/recovered behavioral checks. Oracles are consulted after recovery; generated source is never repaired by hand.

| Fixture scope | Methods | Behavioral observations |
| --- | ---: | --- |
| Integer arithmetic and field mutation | 4 | 81 boundary/overflow operand pairs; constructor exercised |
| UInt32/UInt64 comparisons | 8 | 200 predicates across 50 operand pairs |
| Scalar struct parameters | 4 | 100 equality/addition results across 50 pairs |
| Signed/logical right shifts | 4 | 260 results across 130 rows |
| Guarded division/remainder | 8 | 596 results across 298 rows; explicit zero/minimum guards |
| Metadata literal | 1 | Repeated string value and object identity |
| Fresh component instances | 3 | 24 typed field observations and five helper outputs |
| Scalar floating predicates | 12 | 2,028 Boolean results across 338 pairs |
| Byte-field zero comparisons | 4 | 519 observations: all byte patterns, Boolean stores, unchanged fields and null receivers |
| Integer extensions | 12 | 1,364 results across 280 rows: all byte patterns and word/dword boundaries |
| Word-field zero comparisons | 4 | 786,438 checks: all short/ushort/char patterns, preserved fields, fresh defaults and managed null calls |
| Scalar binary64 truncation | 2 | 132 signed Int32/Int64 bit-pattern results across 66 inputs, including both limit boundaries, infinities and quiet NaNs |

These twelve fixtures have native acceptance receipts for 66 methods. At the scalar-truncation/exception-region checkpoint, all 64 previously accepted methods pass strict recovery and typed IL verification with unchanged method bodies and all 68 source/configuration files. The two added conversion methods pass a fresh complete native round trip. Finite fixture results establish neither general application equivalence nor support for all runtime initialization, exception or partial-register behavior. Receipts authenticate the tool snapshot, player inputs, build settings and generated artifacts locally. The integrated runner performs independent declaration comparisons before and after rebuilding; the earlier integer and scalar-struct scopes have equivalent separate comparison evidence.

- Choose a tiny vertical slice with constants, arithmetic, a conditional, a field read/write and a direct managed call. Build it through the exact target, recover it, regenerate source, compile in Unity, rebuild and compare observable results.
- Audit the slice's metadata resolution, calling convention, lifting and IL generation. Remove guessed-value and unsupported-operation substitutions from its verified path. Check stack balance/types and control-flow joins explicitly.
- Make an evidence-based emitter choice: validated CIL plus a compatible C# decompiler, or structured typed IR to C#. Evaluate correctness, Unity syntax, dependency licensing and maintenance with this same fixture. Share managed models; avoid divergent declaration logic.
- Emit a minimal project with real target references and assembly boundaries. Keep diagnostic `diffable-cs` behavior distinct from a new compilable-source contract.
- Establish the headless validation harness now, with fresh output directories, bounded execution, logs, exit codes, completed compilation/build signals and machine-readable results.
- Regenerate and revalidate from clean player inputs. Include a deliberately unsupported case to prove the reporting/strict validation path does not claim success for it.

Exit evidence: one end-to-end, player-input-only source recovery with exact Unity compilation, Windows x64 Release IL2CPP rebuild and passing behavioral comparisons. No fallback body or manual source repair is accepted in the verified slice.

## Milestone 2 — Metadata and source declaration fidelity

Status: in progress. A separate exact-target declaration fixture preserves 10 types, 35 methods, 25 fields, seven properties, two events, six generic parameters and 27 attributes through stripping. A metadata comparer checks identities and declaration facts independently of method bodies; its own mutation checks detect changed declarations. Correcting declared packing removes one measured layout defect. Two raw differences remain: a zero declared size becomes the effective native size, and an original marshaling descriptor is unavailable. Exact-editor/native reflection probes retain these differences rather than normalizing them away.

A new controlled packed-layout fixture isolates the size ambiguity. Its original source compiles in the supplied Windows 2021.3.35f1 editor and builds a Windows x64 Release IL2CPP player. Both unstripped and stripped managed assemblies retain declared sizes 0, 6 and 8 for otherwise identical pack-2 structs. The omitted-size and explicit-six-byte cases have identical inspected player type flags, packing, native and instance sizes, field types, attributes and offsets; the explicit-eight-byte control increases the native size. Thus those player facts cannot determine whether the source omitted `Size` or declared the natural size. This is declaration evidence only: regenerated source, strict rejection of the unknown size and behavior have not yet been validated for this fixture.

The fixture now pairs three reference classes with the same fields and pack. A fresh exact-target original build retains declared sizes 0, 6 and 32 through managed stripping. The omitted and explicit-six-byte classes have identical inspected version-29 player flags, six-byte native size, 22-byte instance size and field offsets; the 32-byte control increases native and instance sizes. The player distinguishes reference classes from value types, but not omitted from explicit natural `Size` within either group. At that checkpoint the DLL writer did not emit reference-class ClassLayout. A subsequent fresh original fixture build and nonstrict player-only export confirmed that all three generated class declarations omitted Pack and Size. That export also has unresolved constructors; it is diagnostic output, not accepted recovered source or behavior.

The version-29 player-only source report records `DECL002` and `UnknownDeclaredClassSize` for selected non-enum value types with a nondefault class-size flag, including unavailable or zero native sizes. It separates the raw native size from the writer's emitted size candidate and marks declaration fidelity partial, while keeping source-generation and method-recovery dispositions separate. Reference-class layout is checked separately: schema 5 records raw native size, both layout-default flags and player packing values for selected reference classes with nondefault packing or class-size flags. At that checkpoint the DLL writer omitted their ClassLayout rows. `DECL003` and `UnknownDeclaredClassSize` apply only where the class-size flag is nondefault; a packing-only row does not claim an unknown authored Size. Each omitted row added `SOURCE011`, marked source generation partial unless it was already incomplete, and made the strict source gate reject when reached. Synthetic report tests covered both size-flagged and packing-only cases; strict recovery of the exact class fixture stopped earlier at its unresolved constructors. A fresh exact-target nonstrict player-only export reported three affected classes, each with nondefault packing and class-size flags. A metadata-only scan of thirteen previously accepted exact-target fixture inputs found zero newly flagged reference classes. The earlier value-type metadata scan likewise found zero new flags. A strict source-only recheck of the accepted three-method sequential-null-guard fixture still emitted all three methods with zero source diagnostics and six source/configuration files byte-identical to its accepted Unity round trip; the method recovery report was byte-identical and the source report alone gained schema version 5 and empty class-layout metadata arrays. This recheck did not repeat Unity compilation, native rebuilding or behavior. The class-layout fixture has no accepted recovered-source, exact Unity rebuild or behavioral result.

The value-type size ambiguity now also marks source generation partial and emits `SOURCE012` for each affected selected assembly. Strict source output rejects the native-size-derived declaration candidate when it reaches this gate; a focused three-test synthetic regression passes for unknown size and an unaffected control. This does not recover the authored size, and the exact packed-layout fixture has not gained a recovered-source, native-rebuild or behavioral result.

A later bounded writer change emits a reference-class ClassLayout row only when version-29 metadata supplies a nondefault valid Pack, declares default class size, and retains Sequential or Explicit layout. It uses `Size=0`; it does not copy an effective native size into an unknown authored declaration. The source report still lists these classes but records the pack-only row as emitted without `SOURCE011`; any ambiguous class-size flag remains `DECL003`, partial, and strictly rejected. Six focused tests pass, including a synthetic C# 9 decompilation that emits `StructLayout` with `Pack = 2` and no Size. The exact-target collision fixture has nondefault class-size flags on all three reference classes, so this does not clear its ambiguity. No exact-player pack-only round trip, Unity source compilation, native rebuild or behavior result has been established for the new case.

A read-only scan of both retained broad player inputs and the available exact-target fixture player inputs found no reference class satisfying that pack-only gate. Every observed nondefault packed reference class also had a nondefault class-size flag and remains strictly rejected. This scan establishes the current gate's zero measured input coverage; it does not establish that the emitted pack-only declaration compiles or preserves behavior in the exact Unity player.

A fresh neutral pack-only control tested that gate with the supplied Windows Unity 2021.3.35f1 editor and a successful x64 Release IL2CPP original build with zero errors. Five distinct reference-class layouts authored with `Pack = 2` and omitted `Size` retain managed ClassLayout `Pack = 2, Size = 0` before and after stripping, yet every corresponding version-29 player type reports both nondefault packing and nondefault class-size flags. Explicit zero-size and larger-size controls have the same nondefault class-size flag. Strict player-only source recovery emits all seven selected constructors but exits with `DECL003` and `SOURCE011` because the authored size cannot be established; all seven ambiguous ClassLayout rows are omitted. This exact-target control confirms that an authored pack-only declaration does not necessarily satisfy the writer's conservative pack-only gate. There is no accepted regenerated source, native rebuild or recovered behavior for this control.

A separate marshaling fixture proves that Boolean `I1` and `U1` descriptors yield identical field type, flags and marshaled-size evidence. Size alone cannot recover the original annotation. Source output explicitly reports missing marshaling descriptors and strict mode rejects that incomplete declaration. No guessed `MarshalAs` annotation is accepted.

An independent application comparison now passes with zero differences and zero decode diagnostics: 1,769 types, 12,724 methods, 10,798 fields, 2,664 properties, 42 events, 43 generic parameters, 8,848 attributes and 29 assembly references match the supplied managed backup. Version-29 empty-string parsing, reference preservation and the finalizer mapping resolve all 35 initially measured differences. This is the comparer's declaration projection against that backup, not recovered-body, source-compilation or behavior evidence; a separate unstripped oracle is unavailable.

Declared assembly dependencies now survive emission even when no retained signature uses them. The writer preserves allocated reference rows, with a regression that removes all consumer types before writing and reloading the assembly. A separate seven-type, 13-method exact-target finalizer fixture passes player-only declaration comparison with zero differences: both destructors retain their canonical `Object.Finalize` mappings, while inherited finalizers and unrelated virtual methods gain no invented mappings.

A separate enum/interface/struct assembly passes strict player-only source recovery, typed IL verification, declaration comparison and exact macOS editor compilation. Its sole interface method correctly remains bodyless; zero methods are counted as recovered behavior. Explicit known input assemblies can therefore preserve declarations without invented implementations, while missing required native bodies and other exclusions still fail strict validation.

A separate eight-type attribute/parameter fixture exposed incorrect named-member types and boxed Type/array serialization. The writer fixes preserve all twenty emitted attributes without decode diagnostics, including null versus empty arrays and overloaded constructor identities. The complete declaration comparison still fails with eleven fact differences: two absent return rows/attributes, a readonly return signature lost in the native type record, and serialized Type-name qualification. These measured gaps are retained explicitly; passing the attribute serialization regression is not a complete declaration-fidelity result.

For player-derived metadata version 29, an unmodified by-reference return cannot establish whether its source declaration was `ref` or `ref readonly`: the exact fixture loses the required modifier and its return-parameter row. The source exporter now reports `SOURCE008` and marks that assembly partial; strict output rejects it. Authored managed signatures with known modifiers remain separate. A synthetic regression verifies this disposition, but it does not recover the eleven missing declaration facts or add a Unity compilation claim.

Version-29 player metadata also lacks a method-linked return-parameter token. The exact attribute/parameter fixture confirms that two original sequence-zero return rows and their attributes survive managed stripping but are absent from the player attribute ranges and player-derived output. A further native check found that its mutable and readonly byref methods share one return type index with zero return flags. A gap in explicit-parameter tokens spans those two zero-parameter methods, so it cannot assign the omitted row to either one or recover its attributes. The source report therefore records `UnknownInPlayer` for return-row presence and return custom attributes on each selected player method, with a separate `DECL001` declaration diagnostic and partial declaration-fidelity status. This does not add guessed rows or attributes, and it does not change source generation or strict method recovery. A fresh original synthetic fixture compiled in the supplied Windows editor and built as a Windows x64 Release IL2CPP player; its player-only source report marked all 15 selected methods' return-row/attribute presence unknown. The focused player-input regressions passed, while the independent declaration comparer retained eleven differences against the stripped managed oracle with zero decode diagnostics. Existing strict roundtrip receipts retain their IL, Unity compilation, native build and behavioral results. Their zero-difference declaration results are independent comparisons against each fixture's stripped managed oracle; player-only full 1:1 return declaration fidelity remains unverified. The declaration comparer remains the strict, oracle-bounded declaration gate.

A separate attribute-array fixture verifies retained enum-array identity when boxed as an object. All 25 attributes decode, and its unstripped declaration comparison has zero differences. The stripped-oracle comparison retains one difference group for serialized Type-name qualification; the raw comparison does not normalize that remaining gap away.

The source exporter retains provenance for nonnull `System.Type` values decoded from version-29 player attribute blobs, including nested arrays and named members. The indexed type identity survives; the original serialized type-name qualification does not. Strict output now accepts an affected assembly only when each indexed Type leaf has a resolved context and the affected attribute's retained arguments match its emitted managed signature: constructor and fixed/named argument counts, boxing and array shape, named member signatures, assembly-scoped Type identity, and typed primitive, enum and null sibling values. Unknown sibling shapes fail closed. Missing, ambiguous or dropped emission still raises blocking `SOURCE010`. Successful in-memory emission instead raises nonblocking declaration diagnostic `DECL004`, with the indexed leaf count, while raw qualification remains unavailable; `DECL001` independently retains the return-row uncertainty. This gate does not certify post-serialization output. Focused exact-player regression mutations reject unresolved and wrong-assembly Type values, missing attributes, wrong nested leaves, incorrect box/array/named shapes, and changed retained byte, enum, typed-null and separate named-string siblings. The later sibling check passed its focused exact-player test, .NET CLI build and full solution tests (1,555 discovered; 1,463 passed, 92 optional-input skips, zero failed). Two fresh player-only exports generated byte-identical C#, assembly definitions and package manifests, also matching the retained earlier export; managed DLL bytes varied only in the output module MVID GUID entry, so binary identity is not claimed. The later check has not repeated Unity compilation or native rebuilding. On an earlier one-constructor synthetic control, fresh player-only strict output emitted its sole selected method with no source diagnostics, then its regenerated source compiled in the supplied Windows Unity 2021.3.35f1 editor and rebuilt a Windows x64 Release IL2CPP player. A controlled runtime reflection observer reported the same `System.String` Type object in original and rebuilt players; both editor and player arithmetic harnesses passed 81 observations. The rebuilt stripped managed assembly had zero declaration-projection differences from the original stripped oracle. The synthetic `link.xml` and observer were auxiliary validation inputs, not recovery inputs. A comparison of the earlier recovered managed assembly against the stripped oracle exposed one raw qualifier difference, while the unstripped oracle comparison had none; these results do not recover the original SerString spelling. Return-parameter custom attributes are not exposed by the current analyzed model and remain outside this provenance scan.

- Verify registration, metadata tables, type/member ownership and address mappings against exact-target fixtures. Validate bounds and fail diagnostically on inconsistent inputs.
- Preserve namespaces, assembly identities, nested/generic types, constraints, inheritance, interfaces, overrides, explicit implementations, overloads and accessibility.
- Recover constructors, static constructors, properties/indexers, events, delegates, enum backing types, parameter modifiers/defaults, constants and supported attributes accurately.
- Verify field layout, value types, reference fields, generic substitution and static/thread-static storage. Do not confuse native object headers or runtime offsets with source-level layout directives.
- Emit valid identifiers and stable collision mappings. Preserve externally observable names, reflection behavior and serialized field identity; report cases that cannot be represented faithfully in C#.
- Use Unity-compatible syntax and the correct API/reference assemblies, package dependencies and assembly definitions. Respect Unity's [C# compiler restrictions](https://docs.unity3d.com/2021.3/Documentation/Manual/CSharpCompiler.html); the host tool's language version is unrelated to the output contract.

Exit evidence: declaration comparisons pass for the declared fixture set; generated application assemblies compile in the exact editor without duplicate engine/framework types or hidden reference substitutions. Incomplete bodies remain separately reported.

## Milestone 3 — Windows x64 Release semantics

Status: in progress through the established end-to-end harness. The arithmetic, unsigned-predicate, scalar-struct and right-shift scopes pass the complete native round trip. Parameter identity, by-reference writes and constructor-fusion boundaries have executable synthetic IL regressions. Dead-code elimination preserves potentially throwing evaluations; propagation and copy coalescing respect effects, aliasing, type identity and native widths. Native operations and opaque calls invalidate overwritten status flags; only proved flag values can survive into managed output, and unused unknown flag results can be removed.

Constructor callsite resolution now requires a newly allocated owner's constructor to be bound to the native target address; a same-arity constructor found only in the owner's metadata is insufficient. An address-bound generic definition constructor may still be specialized with the allocation's known type arguments. For a call on `this` inside a constructor, the receiver pass rejects shared owner/base constructor aliases, mixed constructor/member aliases and a non-immediate ancestor constructor. It can choose the unique immediate-base constructor among an otherwise base-only alias chain, including a generic definition's proven open-self receiver. These are call-identity gates; they do not establish the original initializer syntax or validate arbitrary constructor bodies. All 21 focused synthetic callsite checks pass. The .NET 10.0.107 Release solution builds with zero errors and four existing prerelease-package warnings; the full solution test run discovers 1,582 tests, with 1,481 passed, 101 optional-input skips and zero failures. Fresh strict player-only source rechecks emit both selected folded constructors and all eleven selected generic Boolean-getter fixture methods; their recovery reports, source reports and seven source/configuration files per fixture are byte-identical to the committed baseline. This resolver checkpoint does not repeat Unity compilation, native rebuilding or behavior.

The scalar-struct bridge preserves the managed parameter and loads its single proved primitive field. It requires a Windows x64 by-value parameter, default sequential blittable layout, matching 32/64-bit size and legal field access. Padded, multi-field and reference-containing neighboring fixtures all reject strict recovery. Right-shift emission distinguishes sign fill from zero fill, retains native operand width, masks the count to five or six bits and rejects unknown widths or unproved destination extensions. A bounded native byte/word-memory comparison against zero now requires an unchanged exact-width field layout and a single captured read. Volatile barrier/test shapes, packed or overlapping fields, 32-bit address truncation and indexed/RIP-relative operands remain rejected. General narrow comparisons, partial-register semantics and the broader groups below remain active work.

Division recovery now requires a native basic-block proof that the high half of the implicit dividend is zero or the correct sign extension. It distinguishes DIV/IDIV, snapshots operands before overwriting either result, preserves 32/64-bit width and rejects unproved wider dividends. Executable IL tests cover unsigned high bits, zero divisors and signed quotient overflow even when only the remainder is used; native end-to-end acceptance is the guarded eight-method scope above.

Broad metadata/class-initialization deletion and literal-argument call substitution have been retired. A positive exact-profile proof accepts only a closed metadata-literal guard with a known helper and matching later literal load; Boolean-field branches and class initializers remain explicit. Recovery-region cleanup also retains potentially throwing arithmetic, memory/array evaluations and escaped values. A computed-address store alone no longer establishes a GC bitmap write; that optimization requires further provenance before it can resume.

Unknown native calls also remain explicit: transitive exception-name strings and call-chain hints no longer substitute guessed allocation/throw behavior. A fresh verification of all seven accepted scopes still recovers 32 of 32 methods with valid typed IL and 44 byte-identical generated source/configuration files. This reuses the authenticated native-run evidence because those generated inputs are unchanged.

Explicit null/bounds exception guards now remain until a matching managed access proves that their condition and exception ordering are preserved. Four executable regressions prevent exception-name-only cleanup from changing a throw into an ordinary return. Scalar floating predicates have a separate width and four-outcome representation, with 12,544 executed managed comparisons covering all sixteen outcome masks; the twelve-method native round trip also passes. Its vectors cover finite values, infinities, both zeros, subnormals and quiet NaNs. Memory operands, signaling-NaN exception status and externally altered floating control state remain separate scopes.

Integer extension now records source width, result width and sign/zero fill explicitly. A closed native parameter-to-return proof preserves EAX zeroing separately from RAX sign extension; unproved conversion forms no longer become ordinary moves. Its twelve-method fixture passes strict recovery, typed IL verification, both declaration comparisons, exact-editor compilation and the recovered native round trip with 1,364 results across 280 rows. Retiring ordinary MOVZX moves initially exposed an unproved shift-count setup. A bounded proof now retains the actual Int32 mask and emits an explicit low-byte zero extension only when every use is a supported register shift count. At the validated word/shift checkpoint, all 64 selected methods pass strict recovery and typed IL: ten scopes (60 methods) have identical source/configuration and method IL, and the four changed shift methods pass a fresh complete native roundtrip with 260 results. Comparisons distinguish method bodies from whole-DLL hashes, which may differ with module identifiers.

Native body boundaries also fail explicitly when no terminal path is proved, rather than inventing a return. Real returns and closed control flow leave that diagnostic unreachable. Rewritten throws repair block successors and positional phi inputs before unreachable code is removed. Missing receiver-local warnings clear only when independent native reads/writes prove the incoming receiver unused; ambiguous mappings keep their diagnostics. Stack analysis now checks exit paths independently, preserves warnings on unbalanced returns, allows explicit throws to unwind, rejects inconsistent stack positions at real joins and checks offset overflow. Eleven focused regressions pass; this stack change is later than the completed native checkpoint.

A separate scalar-truncation baseline preserves two binary64-to-signed-integer methods. The exact Windows editor and original Release player each pass 132 bit-pattern results across 66 inputs, including representable neighbors of both integer limits, infinities and quiet NaNs. Native inspection confirms the two register conversion/return bodies. A bounded native proof now emits an explicit binary64-to-signed conversion with guarded managed IL, preserving signed minimum output for NaN and overflow without relying on unspecified managed cast results. Ninety-one focused checks pass, including actual native bodies, independent bit-pattern calculations and rejection controls. Both methods now pass strict player-only recovery, typed IL verification, both declaration comparisons with zero differences, exact Windows editor compilation and behavior, and a rebuilt Release IL2CPP player with all 132 results. This scope verifies result bits with masked floating exceptions; floating status flags and externally changed control state remain separate.

Exact-profile x64 recovery now checks the PE exception directory and every reachable native instruction before lifting. Malformed, unproved chained or handler-bearing unwind regions remain unsupported; frame-free leaves require independent register and control-flow checks. The initial sixty-two focused checks included two original native catch methods that the production lifter explicitly rejected. This protects reporting while catch/finally recovery remains unfinished. A separate null-throw helper investigation found constructor-exception suppression in the runtime, so ordinary allocation/construction/throw lowering remains disabled.

The exact target runtime null-check helper now has a native instruction, metadata-identity, read-only literal and unwind proof. Its operation remains explicit until an SSA guard proves that the null arm is exclusive and that the other arm reaches the same receiver's nonvirtual call without moving side effects across the check. The emitter then retains the implicit receiver check through managed `callvirt`; an uncoalesced helper remains a strict failure. The four-method loop/direct-call fixture passes strict player-only recovery, typed IL, zero-difference declaration comparisons before and after rebuilding, exact Windows editor compilation and a Release IL2CPP player rebuild. Original and recovered editor and player behavior each cover 983 observations, including null receivers and zero-count bypasses. The 187 focused checks also pass. This is a bounded native-call and null-guard proof, not catch/finally or general exception-construction support.

A synthetic `int[]` read control first passed strict player-only recovery, typed IL verification, zero-difference declaration comparisons before and after rebuilding, exact Windows editor compilation and a Release IL2CPP player rebuild. The original and recovered editor and player each passed 28 observations, including null arrays, negative indices and upper-bound failures. A closed native proof checks the successful element load, both distinct nonreturning runtime exception helpers, and the caller's unwind region before emitting an implicit managed array access.

The same synthetic assembly now adds a parameter-supplied `int[]` element write. Its exact player has the same null-first and unsigned bounds guards followed by a 32-bit indexed store. A shared proof checks either the load or store, both distinct nonreturning helper identities, method signatures and the caller unwind region. The terminal bounds-helper call is valid with or without a following padding instruction; a fixture rebuild exposed that boundary variation in the read method. Both selected methods pass strict player-only recovery, typed IL, zero-difference declaration comparisons before and after rebuilding, exact Windows editor compilation and Release IL2CPP player rebuild. Original and recovered editor and player runs each pass 56 independent observations, including null arrays, negative indices, upper-bound failures and stored signed values. This proves only the bounded `int[]` access scope, not other element types, exception-message or stack-trace identity, or general bounds-helper lowering.

The same exact 32-bit native access proof now accepts unchanged `uint[]` element metadata and return/store signatures alongside `int[]`. A fresh four-method fixture passes player-only strict recovery, typed IL, zero-difference declaration comparisons before and after rebuilding, exact Windows Unity 2021.3.35f1 source compilation and a Release IL2CPP player rebuild. Original and recovered editor/player runs each pass 112 observations, including unsigned high-bit/max values, null arrays, negative indices and upper-bound failures. No other element width, array rank, exception-message or stack-trace identity, or general bounds-helper lowering is established.

The same closed proof now accepts signed 64-bit `long[]` reads and writes only when the exact native access uses an eight-byte element scale, full-width result/value registers, unchanged Int64 metadata and the same independently identified terminal helpers. Binary Ninja and the native decoder agree on the two control bodies. A fresh six-method player-only recovery passes strict mode, typed IL, both zero-difference declaration comparisons, exact Windows Unity 2021.3.35f1 compilation and a Release IL2CPP player rebuild. Original and rebuilt editor/player runs each pass 168 observations, including 64-bit extrema, null arrays and negative/out-of-range indices. At this checkpoint unsigned 64-bit and other widths/ranks remained unverified.

The exact proof now also admits unchanged `ulong[]` element metadata with an eight-byte native access and full-width value/result registers. The fresh eight-method fixture passes player-only strict recovery, typed IL, both zero-difference declaration comparisons, exact Windows Unity 2021.3.35f1 compilation and a Release IL2CPP rebuild. Original and rebuilt editor/player runs each pass 224 observations, including unsigned 64-bit high-bit/max values, null arrays and negative/out-of-range indices. Other widths, ranks and general bounds-helper lowering remain unverified.

A synthetic signed `int` instance-field read first passed strict player-only recovery for both its constructor and read method, typed IL verification, zero-difference declaration comparisons before and after rebuilding, exact Windows editor compilation and a Release IL2CPP player rebuild. Original and recovered editor and player runs each passed six independent observations, including a null receiver and signed boundaries. The bounded read rewrite requires an exclusive native null-throw arm, an unchanged managed receiver and field layout, and a field load that survives later analysis so managed `ldfld` retains the null failure.

The same synthetic class next added a signed `int` instance-field write. The exact player exposes a null-throw arm followed by a 32-bit store on the non-null path. A shared field-access proof requires the exclusive throw arm, unchanged managed receiver/field/value binding, and the surviving `stfld` or `ldfld` at emission; the store value is restricted to a bound managed parameter. All three selected methods passed strict player-only recovery, typed IL, and both independent declaration comparisons with zero differences. Original and recovered Windows editor and Release IL2CPP player runs each passed 12 observations, including null receivers and signed boundaries; the rebuilt source contained the field assignment without manual edits.

A second sealed synthetic class extends the same null-guard proof to signed `long` fields. The exact player performs 64-bit loads and parameter-supplied stores behind the proved nonreturning null arm; layout validation requires the unchanged eight-byte field and excludes overlaps. All six selected methods across the signed 32/64-bit fixture pass strict player-only recovery, typed IL and both independent declaration comparisons with zero differences. Original and recovered Windows editor and Release IL2CPP player runs each pass 24 observations, including null receivers and signed 64-bit boundaries. This remains a bounded ordinary field-access claim, not volatile/concurrent semantics, other field types, exception messages or stack traces.

The field fixture now also clears an ordinary Boolean instance field to `false`. An exact seven-instruction Windows x64 shape proves the byte store, null branch, nonreturning runtime helper, unique method binding and handler-free caller unwind region before `stfld` replaces the throw arm. All eight selected methods pass strict player-only recovery and typed IL; both independent declaration comparisons have zero differences. Original and regenerated Unity editor and Release IL2CPP player runs each pass 27 observations, including both initial Boolean values and a null receiver. General byte stores, volatile/concurrent semantics and other null-diamond shapes remain unsupported.

The fixture now clears a signed `int` field through one reference field. An exact eight-instruction Windows x64 proof binds the reference load, guarded 32-bit zero store, two unchanged managed field layouts, null-helper identity and caller unwind region before retaining managed `ldfld` and `stfld` in the emitted method. All ten selected methods pass strict player-only recovery and typed IL; both declaration comparisons have zero differences. Original and regenerated runs in the supplied Windows Unity 2021.3.35f1 editor and Release IL2CPP player each pass 32 observations, including null inner and outer references. This does not prove arbitrary nested access, other stored values, concurrent/volatile semantics, exception details or source syntax erased by compilation.

A direct signed `int` zero store through a supplied receiver has the same seven-instruction null diamond as the proved Boolean clear, with a 32-bit store instead of a byte store. The width-aware proof binds the exact native instruction, managed field and null failure before accepting the emitted `stfld`. All eleven selected methods pass strict player-only recovery, typed IL and both zero-difference declaration gates. Original and regenerated runs in the supplied Windows Unity 2021.3.35f1 editor and Release IL2CPP player each pass 36 observations, including a null receiver and signed boundary initial values. Other immediates, widths, field kinds and guard shapes remain unverified.

A direct Boolean field read uses a guarded `movzx eax, byte [rcx+field]`. A closed proof binds that load to an unchanged Boolean field and return type, verifies the null-helper and unwind coverage, and lowers only that extension to a managed field read. All twelve selected methods pass strict player-only recovery, typed IL and both zero-difference declaration gates. Original and regenerated runs in the supplied Windows Unity 2021.3.35f1 editor and Release IL2CPP player each pass 39 observations, including both Boolean values and a null receiver. Other integer extensions and byte-field reads remain unsupported without their own proof.

An instance method that reads a Boolean field from its parameter uses `RDX` while its incoming `this` value in `RCX` is unused. The exact seven-instruction proof now binds the tested and loaded register to that parameter and proves the receiver unused across the otherwise branching native body. All fourteen selected methods pass strict player-only recovery, typed IL and both zero-difference declaration gates. Original and regenerated runs in the supplied Windows Unity 2021.3.35f1 editor and Release IL2CPP player each pass 43 observations, including a null field owner and a null instance receiver. This does not establish a general unused-receiver or `RDX` field-read rule.

A bounded array-returning instance call now preserves an `int[]` parameter and return, the callee's field increment and the null-receiver failure. The direct native tail jump is classified against the method's half-open byte range; a jump to its exact end is external. Array type identity uses the unchanged element context when the array wrapper is rematerialized. The three selected methods pass strict player-only recovery, typed IL and both zero-difference declaration gates. Original and recovered Windows editor and Release IL2CPP player runs each pass seven observations, including array identity and contents, empty and null arrays, signed field-increment overflow and null receivers. The proof does not cover arbitrary arrays, indirect tail calls, exception-message identity or other native call shapes.

The expanded array-call fixture passes a derived receiver to the same nonvirtual base method. Null-guard coalescing now accepts only an unchanged ordinary class chain from that receiver to the declaring type; it still requires the exclusive proven null arm, unique unchanged native target binding, receiver identity and effect ordering. The exact player folds empty base and derived constructors into one reachable two-instruction tail thunk, although the reported metadata byte span includes unrelated following code. Constructor recovery validates that reachable external transfer, the immediate base constructor's shared binding and native exception-region coverage. All five selected methods pass strict player-only recovery, typed IL, and both zero-difference declaration comparisons. Freshly regenerated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as x64 Release IL2CPP. Original and recovered editor/player runs each pass nine observations, including base and derived calls, array identity, field effects and null receivers. This does not establish general virtual dispatch, other inheritance shapes or exception-detail identity.

The same fixture now calls the nonvirtual method through two separate receivers in sequence. Its exact x64 player branches both receiver checks to one terminal runtime null helper. The coalescer accepts a shared helper only when the other incoming edges are direct conditional branches and the helper has no effect beyond its proven nonreturning throw; intermediate null-arm blocks remain exclusive. Both managed `callvirt` instructions retain the ordered null failures and side effects. All six selected methods pass strict player-only recovery, typed IL, both zero-difference declaration gates, fresh exact Windows Unity compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass twelve observations, including null-first and null-second cases and the first call's field effect before the second failure. Other shared helper shapes remain unverified.

An exact seven- or eight-instruction x64 proof accepts a guarded `System.String` field read from an unchanged reference type, directly through a supplied owner or through one unchanged reference field of `this`. It binds the 64-bit load, branch, nonreturning null helper, native unwind, method-specific native binding, field layout and receiver provenance before allowing managed `ldfld` to retain the null failure. Derived runtime owners retain the base field identity and offset; reference-field writes, deeper access chains and different native shapes remain unverified. The expanded exact-target fixture includes base and derived owners and retains two derived marker fields. The original Windows editor and Release IL2CPP player each pass sixteen observations, including noninterned reference identity, empty and null strings, null owners and unchanged derived markers.

The exact target folds each empty base/derived constructor pair into the same two-instruction tail thunk to `Object::.ctor()`. The first six-method recovery emitted calls from derived constructors straight to `Object::.ctor()`; ILVerify rejected both because managed constructors must initialize `this` through the immediate base constructor. A bounded analysis pass now rewrites only a parameterless derived constructor with the shared native address, unchanged metadata, the exact external tail thunk and no other managed operation. It calls the immediate base constructor once. All six selected methods now pass strict player-only recovery, typed IL and both zero-difference declaration gates. Freshly regenerated source compiles in Windows Unity 2021.3.35f1, rebuilds as Release IL2CPP and matches the original on all sixteen editor and player observations. Other constructor shapes and source details erased by native optimization remain unverified.

The reference-field fixture now places unchanged `int[]` fields before and after the string and parent-reference fields. Resolving their metadata can rematerialize array type wrappers, so object identity between two resolved field types is not a valid unchanged-layout check. The layout proof now checks the explicit field-type override while retaining raw modifier, attribute, offset, extent and overlap checks. With uniquely bound native read bodies, the old tool fails both reads and the corrected tool emits all six methods. The fresh player-only recovery passes typed IL, both zero-difference declaration gates, exact Windows editor compilation and Release IL2CPP rebuilding. Original and rebuilt editor/player runs each pass seventeen observations, including neighboring array contents and identity. Other shared native-address patterns remain unresolved.

An expanded exact-target control places two more string reads at a shorter field offset. Their native bodies are folded with methods from other assemblies, so the general call-target signature check still rejects ambiguous bindings. The exact string-read proof can instead bind each method through its own unchanged signature, receiver field and complete seven- or eight-instruction native body while requiring that the method occur in the native-address map. Both shared reads and the prior unique-body reads now pass together: ten selected methods emit from player inputs, typed IL and both zero-difference declaration gates pass, and clean generated source compiles and rebuilds in Windows Unity 2021.3.35f1 Release IL2CPP. Original and rebuilt editor/player runs each pass twenty-six observations, including null owners, null field values, reference identity and neighboring arrays. This does not establish a rule for arbitrary folded native bodies or other return types.

The same exact native reference-read proof now accepts unchanged `System.Object` fields and return signatures as well as strings. The expanded control reads an object field directly and through one reference field; it checks boxed integer and string object identity, null field values and null receivers. All twelve selected methods emit from player inputs and pass typed IL and both zero-difference declaration gates. Fresh generated source compiles and rebuilds in the supplied Windows editor with Release IL2CPP; original and rebuilt editor/player runs each pass thirty-five observations. Other reference types, generic fields, writes and native shapes remain unverified.

The reference-read control now also returns an unchanged `int[]` field directly or through one reference field. Array wrappers may be rematerialized, so the proof matches the same non-generic Int32 element type and raw array metadata rather than wrapper identity; the null-guard coalescer uses that bounded match for the retained `ldfld`. On the exact original player, the previous tool fails only these two reads while the other twelve emit. The corrected player-only recovery emits all fourteen methods, passes typed IL and both zero-difference declaration gates, and its freshly generated source compiles and rebuilds in Windows Unity 2021.3.35f1 Release IL2CPP. Original and rebuilt editor/player runs each pass forty-four observations, including array contents, empty/null arrays, reference identity and null owners. Other array elements, ranks, writes and native shapes remain unverified.

The same exact seven-instruction proof now binds a direct field read from a nonstatic, parameterless method's `this` receiver as well as a static method's first reference parameter. Its unchanged class and field layout, return metadata, native null helper and handler-free body still have to match. Three additional methods return string, object and `int[]` fields from `this`. All seventeen selected methods emit from player inputs, pass typed IL and both zero-difference declaration gates, and freshly generated source compiles and rebuilds in the supplied Windows Unity 2021.3.35f1 editor as a Release IL2CPP player. Original and rebuilt editor/player runs each pass fifty-three observations, including returned reference identity, null field values and null instance receivers. Other receiver types, signatures and native shapes remain unverified.

The bounded reference-read proof now accepts an unchanged ordinary application-class field and return type, with the same exact null diamond and field-layout requirements. A class field read through `this` and a supplied receiver exercise reference identity, null field values, null owners and derived owners. The earlier tool emits the `this` read but fails the supplied-receiver method at an uncoalesced null guard. The corrected player-only recovery emits all nineteen selected methods, passes typed IL and both zero-difference declaration gates, and its freshly generated source compiles and rebuilds in the supplied Windows Unity 2021.3.35f1 editor as a Release IL2CPP player. Original and rebuilt editor/player runs each pass sixty-one observations. Generic, interface, value-type and other native reference-read shapes remain unverified.

The same exact native field-read proof now admits `string[]` and `object[]` returns alongside the previously accepted `int[]`, requiring the unchanged array element identity and raw SZARRAY metadata. Six added methods read these fields through a parameter, through `this`, or through one reference field. The prior tool emits both `this` methods but fails the other four on uncoalesced null guards. Corrected player-only recovery emits all twenty-five selected methods, passes typed IL and both zero-difference declaration gates, and clean generated source compiles and rebuilds in the supplied Windows Unity 2021.3.35f1 editor as a Release IL2CPP player. Original and rebuilt editor/player runs each pass eighty-seven observations, including array contents and identity, empty and null arrays, and null owners. Other element types, ranks, writes and native shapes remain unverified.

A two-method exception-region control now passes the exact Windows editor and original Release IL2CPP player with 14 observations each. It exercises an explicit managed throw caught by type and a finally side effect on normal and exceptional exits. Both player-only recoveries currently fail strict validation at the native-handler gate, as required; neither is counted among accepted methods. Its x64 unwind entries carry both handler flags, and [Microsoft's x64 exception format](https://learn.microsoft.com/en-us/cpp/build/exception-handling-x64?view=msvc-170) leaves the following handler data language-specific. A preliminary divide-by-zero fault variant passed editor behavior but crashed in the Wine player; the passing control uses an explicit managed throw to isolate EH recovery from host fault handling. That failed native run remains local and supplies no behavioral acceptance evidence.

The unwind reader now preserves validated handler flags, executable handler identity, and the address of file-backed read-only handler data. Synthetic malformed-pointer and mutable-data controls reject invalid evidence. Both exact-target methods expose distinct handler data through this structural reader and still fail strict recovery; the language-specific try, catch, and unwind maps have not yet been established as managed exception regions. This checkpoint adds no recovered method or Unity rebuild claim.

A bounded reader now parses the non-separated frame-handler-4 map structure behind that handler data. Each exact-target control has one try entry, one handler entry and two instruction-state entries; the catch control has two unwind states, while the finally control has three and an explicit native unwind action. Synthetic tests exercise all compressed integer widths, malformed maps and section permissions. A native handler/type-descriptor address is not proof of a managed catch type, and an unwind action is not yet a managed finally clause. Both methods remain strict failures with no emitted C# or rebuilt-player result.

The EH4 reader now resolves each unwind entry's backward byte link to a prior entry or the state-minus-one sentinel, following the supplied MSVC 14.29 header. Malformed links fail parsing. The exact finally control's protected state links through a cleanup entry to the sentinel; native inspection identifies a counter increment and conditional rethrow in that cleanup. This is still structural evidence for the finally path, which remains a strict recovery failure.

The exact catch and finally controls share the same native C++ handler type descriptor despite having different managed clauses. Native inspection finds a managed-class check in the catch funclet and a separate counter-increment cleanup action for the finally path; the generated C++ oracle corroborates that interpretation after player-only analysis. The EH map field is labeled as a native type descriptor, and the exact-target regression guards against treating it as a managed catch identity. Both methods still fail strict recovery: no managed exception clauses, source build or recovered-player behavior are claimed.

A bounded x64 catch-funclet proof now reads the file-backed handler region, matches the metadata initialization and class-assignability check, and resolves the checked managed class from player metadata. It also verifies one in-region continuation with a signed 32-bit constant return. The exact-target control identifies its checked exception class and continuation result; the finally control has no such proof. This establishes pieces of the catch path, not a complete managed clause or its exception flow. Both methods remain strict failures, with no recovered source compilation, native rebuild or behavioral acceptance.

The catch control now also matches its complete signed-division native body: the zero-divisor branch allocates the checked class, calls its player-metadata constructor, and passes the same exception and caller method metadata to a raise wrapper. The nonzero branch performs signed division; the native continuation returns the proved constant. Small wrapper proofs bind allocation and exception raising to the installed runtime's VM entry points and prove the null guard has no effect for a nonnull object. A bounded funclet-flow proof checks that a matched class reaches that continuation while an unmatched class reaches a C++ throw helper. That helper's forward native paths reach the player PE's named `RaiseException` import with `EXCEPTION_NONCONTINUABLE` set; [Microsoft documents](https://learn.microsoft.com/en-us/windows/win32/api/errhandlingapi/nf-errhandlingapi-raiseexception) this flag as preventing continuation. Its throw information is checked separately from the EH map's native handler type descriptor. The body proof also checks the EH state map and bounded native alignment padding before emitting a managed catch region. Other catch, filter and finally shapes remain unsupported.

A catch-only synthetic fixture now passes strict player-only recovery for its one selected method, typed IL verification, both zero-difference declaration comparisons, compilation in the supplied Windows Unity 2021.3.35f1 editor, and a fresh Windows x64 Release IL2CPP build. Its original and rebuilt editor/player runs each pass seven independently checked observations, including zero divisors, negative operands and signed division. The original two-method exception fixture still rejects its separate finally method, so this result establishes only the proved catch shape and its tested behaviors.

A fresh strict player-only inventory retained every method identity in each of two independent local corpora. The selected application scopes still have 1,791 emitted of 7,256 methods and 4,380 emitted of 12,724 methods, respectively, with no disposition transitions from the preceding receipt. These counts are recovery dispositions, not behaviorally validated methods; both strict runs fail and neither broad output has a Unity compilation or rebuilt-player result.

The x64 unwind reader now validates direct chained records whose complete runtime-function tuple names an exact handler-free primary record with the same frame register. Reachable cold fragments may share that primary's native-region proof; malformed, nested, handler-rooted, and unrelated chains remain unsupported. An exact-version independent player inspection confirmed a handler-free primary with both a zero-code epilog chain and a nonvolatile-save chain. Synthetic PE and control-flow regressions cover the accepted and rejected shapes. This is structural native evidence only: the two managed exception-control methods still fail strict recovery, and no new Unity source compilation or behavioral acceptance is claimed for the broad transition below.

A later SIMD audit removes eleven unproved scalar aliases: raw-bit and full-vector moves, packed bitwise operations, and incomplete shuffle/interleave expansion. Unsupported cases remain explicit even when their results are overwritten; 56 focused decoding, dead-code and emission checks pass. At this checkpoint all 66 accepted methods across twelve scopes still pass strict recovery and typed IL verification. All 74 source/configuration files and all 66 method-body projections are unchanged against their native acceptance receipts; whole DLL byte identity is not claimed.

The frame-free x64 proof now accepts a near `RET imm16` only when its immediate is zero. In that case it pops the return address with no additional stack adjustment, as specified by the [Intel instruction reference](https://www.intel.com/content/dam/www/public/us/en/documents/manuals/64-ia-32-architectures-software-developer-vol-2b-manual.pdf). A read-only native inspection and 31 focused checks distinguish that encoding from returns that do change the caller's stack. The broader corpus refresh below measures the resulting disposition changes without treating them as behavior validation.

After that return-proof change, a fresh receipt-driven refresh of the current committed CLI accepts 70 of 70 selected methods across thirteen scopes with strict recovery and typed IL. All 80 source/configuration files and all 70 method-body projections are unchanged against their completed native receipts. This refresh did not launch Unity; the loop-call scope has the fresh native round trip above. Whole managed DLL byte identity is not claimed.

The bounded x64 `finally` recovery now requires the complete signed-division body, matching exception type and constructor metadata, an EH4 cleanup link and continuation, a funclet that saves the caught exception into the cleanup object, a native action that increments the original by-reference counter, and a rethrow chain to the player's noncontinuable C++ throw helper. An arbitrary handler or cleanup action cannot pass this proof. The exact two-method catch/finally fixture now passes strict player-only recovery (2 of 2 selected methods), typed IL verification, both zero-difference declaration comparisons, fresh source compilation in the supplied Windows Unity 2021.3.35f1 editor, and a Windows x64 Release IL2CPP rebuild. Original and recovered editor and player each pass 14 observations, including normal and exceptional `finally` exits. This verifies those bounded native shapes and tested behavior; other exception-region forms remain unsupported.

Both independent local player-input corpora were refreshed after this change. All 38,700 and 91,198 input method identities remained present, and no disposition changed. The selected application scopes remain 1,791 emitted of 7,256 and 4,380 emitted of 12,724; their strict runs still fail. Neither complete application source has passed Unity compilation, native rebuilding or behavior comparison. No target-specific symbols or evidence from either corpus are used in the recovery rule.

The next shared failure category is an uncoalesced runtime null guard. A bounded player-only proof now accepts an instance method that loads a reference receiver and one or two ordinary call arguments from separate fields of the same owner, branches exclusively to the proved target-runtime null helper when the receiver is null, and otherwise tail-calls one uniquely bound nonvirtual managed method. It checks the complete file-backed x64 instruction region, field widths and layout, metadata identity and managed call signature; other field-call shapes remain unresolved. The expanded neutral fixture has eight selected methods and passes strict recovery, typed IL verification, both zero-difference declaration comparisons, exact Windows editor source compilation, and a Windows x64 Release IL2CPP rebuild. Original and recovered editor/player runs each pass sixteen independent observations, including null receiver, null owner and null array argument cases. A fresh player-only audit retained all 38,700 and 91,198 input method identities; five and two methods, respectively, moved from failed to emitted, with no reverse transition. Selected application counts are now 1,796 of 7,256 and 4,381 of 12,724 emitted. Both complete application scopes still fail strict recovery and have no whole-scope Unity compilation, rebuild or behavioral equivalence result.

A separate closed x64 proof accepts a Boolean literal store through one reference field only when the complete file-backed, handler-free body, exact stack unwind, nonreturning null helper, unique method binding, unchanged field layouts and legal C# field access are established. It bounds the outer field offset to retain the null-owner fault and rejects readonly fields. An exact-target volatile-store control showed that player metadata loses its volatile modifier while the native store adds a barrier and does not match this proof; other volatile forms remain unresolved. The expanded unsealed-class field fixture passes strict player-only recovery of all eighteen selected methods, typed IL, both zero-difference declaration comparisons, exact Windows editor source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass fifty-one observations, including both Boolean literals, null owners and receivers, and untouched neighboring fields. Both private-input audits preserved all 38,700 and 91,198 identities with zero disposition transitions; all nine native-shape candidates found across those inputs write private fields of other types and are rejected as potentially uncompilable direct C#. Selected counts remain 1,796 of 7,256 and 4,381 of 12,724 emitted. Neither complete application scope has a Unity compilation or behavioral-equivalence result.

A separate exact-target control confirms that the existing nested Boolean-store proof also accepts an ordinary reference field beyond the x64 short-displacement range. Its native body has eight instructions and a 30-byte file-backed span, with an authenticated null helper and a byte store to a distinct Boolean target field. Strict player-only recovery emits all three selected methods, typed IL verification passes, and each declaration comparison has zero differences against the stripped managed oracle. Regenerated source compiles in the supplied Windows editor and rebuilds as a Windows x64 Release IL2CPP player. Original and recovered editor/player runs each pass four observations covering both Boolean inputs, alias identity, observed neighboring data, null target and null owner. The padding check is an aggregate sum, not complete byte-level preservation. This control does not establish broad application recovery.

The same closed nested Boolean-store proof now admits one unchanged class-reference parameter when its complete native body reads only `this` and the loaded field receiver; the parameter register is unused. A neutral four-method fixture isolates that case. Its original Windows editor and Release IL2CPP player pass 22 observations each, while a frozen pre-change strict player-only run emits the three implicit constructors and rejects the store at the unresolved null guard. Fresh recovery emits all four selected methods with valid typed IL and zero differences in both declaration comparisons across three types, seven fields, four methods and one parameter. The generated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as a Windows x64 Release IL2CPP player with zero errors. Original and recovered editor/player runs each pass all 22 observations, including null and nonnull ignored arguments, repeated writes, neighboring fields and null receivers. Other parameter types, native bodies and volatile or concurrent semantics remain unverified.

A bounded x64 proof now forwards one unchanged signed-Int32-backed enum argument through a reference field to a uniquely bound nonvirtual managed target. It requires the same raw enum identity at both signatures, a complete file-backed null guard and tail call, exact handler-free unwind and null helper, a low owner-field offset, and legal source access. Integer-to-enum substitutions and shared native target addresses remain unresolved. The neutral four-method fixture passes strict player-only recovery, typed IL, both zero-difference declaration comparisons, exact Windows editor source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass eight observations, including signed extremes, both null paths and an unchanged neighboring field. Fresh audits preserve all 38,700 and 91,198 ordered input identities. The first selected private scope has no disposition change; one method in the second selected scope moves from failed to emitted, yielding 1,796 of 7,256 and 4,382 of 12,724 emitted. Both broad scopes still fail strict recovery and have no whole-scope Unity compilation, native rebuild or behavioral-equivalence result.

A separate bounded x64 proof now accepts same-owner static getters of unchanged native-int or class-reference fields when an exact TypeInfo metadata guard leads to a unique 8-byte field load. It verifies the complete file-backed handler-free body, the writable TypeInfo slot, the guard flag in a PE zero-filled writable virtual tail, the installed runtime helper, field offset/layout and absence of a managed class constructor. The neutral three-method fixture passes strict player-only recovery, typed IL, both zero-difference declaration comparisons, exact Windows editor source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass four observations covering first access, signed pointer boundaries, null and reference identity. Both private audits retain all 38,700 and 91,198 ordered method identities. Forty methods in the first selected scope and six in the second move from failed to emitted, with no emitted regressions; selected totals become 1,836 of 7,256 and 4,388 of 12,724 emitted, including the earlier independent enum transition. These broad disposition changes have no whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result, and both strict scopes still fail.

A separate x64 Boolean-call proof accepts a direct uniquely bound managed Boolean return followed immediately by `test al, al` only when the surviving zero flag feeds `je`/`jne` or a register-source conditional move, with at most two intervening flag-neutral register-immediate moves. It derives zero from the managed Boolean call result, never from undefined upper `RAX` bits; full executable handler-free coverage, unchanged signatures, no alternate entry and no other live flag consumer are required. The expanded field-guard fixture passes strict player-only recovery of all nineteen selected methods, typed IL, both zero-difference declaration comparisons, exact Windows editor source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass fifty-four observations, including true, false, null and unchanged-neighbor cases. Relative to the immediately preceding static-getter audits, nine and forty-six full-input methods move from failed to emitted with no reverse transition, and all 38,700 and 91,198 ordered method identities remain. Selected totals become 1,838 of 7,256 and 4,412 of 12,724 emitted. Both broad scopes still fail strict recovery and have no whole-scope typed-IL, Unity compilation, rebuild or behavioral-equivalence result.

A bounded Windows x64 proof removes only nonvolatile XMM6–XMM15 `movaps` stack saves and restores when exact unwind save records, disjoint slots, a stable frame, a complete straight-line handler-free function and its exact `.pdata` end establish ABI preservation. Intervening vector operations still require separate semantic recovery. The same neutral fixture exposed an independent value-type call-emission defect: a by-value struct parameter used as an instance receiver must supply its managed address. The emitter now permits that address only for unchanged, uniquely mapped parameter storage and rejects an unproved SSA copy. The two-method fixture passes strict player-only recovery, typed IL verification, both zero-difference declaration gates, exact Windows Unity 2021.3.35f1 source compilation, a Windows x64 Release IL2CPP rebuild, and five exact-bit floating observations in each original and recovered editor/player run. In isolated XMM-only full-input audits, all 38,700 and 91,198 ordered identities remain; the first corpus has no disposition transition, and two methods in the second move from failed to emitted with no reverse transition. Selected totals become 1,838 of 7,256 and 4,414 of 12,724. Both broad strict scopes still fail and have no whole-scope Unity compilation or behavioral-equivalence result.

A second, separate scalar-float control now recovers a 12-byte sequential three-field value type passed by reference to an unchanged leaf and by value to its caller. The closed x64 proof requires the complete file-backed leaf and caller spans, unchanged public `Single` fields at offsets 0/4/8, unique direct call binding, exact leaf and handler-free caller unwind classification, and six nonvolatile XMM save/restore instructions authenticated by unwind and stack-slot evidence. It retains the three ordered field captures before the by-reference mutation and the three ordered scalar additions; no general SIMD or by-reference call rule is inferred. The exact original Windows editor and Release IL2CPP player each pass 13 bit-pattern observations, including zeros, subnormals, infinities and NaN classification. Two rounding-sensitive sums have distinct editor and player expectations. Strict player-only recovery emits both methods, pinned typed IL verification passes, and both declaration comparisons against the stripped oracle have zero differences over two types, three fields, two methods and four parameters. Fresh recovered C# compiles in the supplied editor and rebuilds as Windows x64 Release IL2CPP; recovered editor and player runs each pass the same 13 stage-specific observations. Unsafe invalid references, altered floating-point control state and other native shapes remain unverified.

A four-method forwarded-`Int32` fixture confirms that the existing ordinary null-guard coalescer already handles one first-argument forwarding tail call with a second unused `Int32` argument. It passes strict player-only recovery, pinned typed IL, both zero-difference declaration comparisons, exact Windows editor compilation, Win64 Release IL2CPP rebuilding and twelve observations in each original/recovered editor/player stage. This is an existing-path validation control, with no new proof gain. A separate five-method fixture with an eight-byte, two-`Single` sequential struct initially had an unrelated target-body failure; after simplifying only that target, the fresh original Release build and thirteen editor/player observations passed. The current-tool strict preproof run emitted four of five selected methods and rejected only the guarded forwarding caller. A shared complete x64 native-body matcher and separate struct signature/ABI proof now bind its file bytes, executable unwind span, runtime null helper, reference field, unique direct target and unchanged register-carried eight-byte value. Exact-player mutations reject changed ABI type, struct and receiver layouts, ambiguous target binding and an alternate native entry; the existing enum proof still passes its focused exact-player test after matcher extraction. Strict recovery now emits all five selected methods, pinned typed IL and both independent declaration comparisons pass with zero differences, generated source compiles in the supplied Windows editor and rebuilds as Win64 Release IL2CPP, and original/recovered editor/player runs each pass thirteen bitwise and null-path observations. This initial acceptance covers a two-instance-float struct without static fields or a static constructor; the distinct static case is validated below. Arbitrary struct passing and null-guarded calls remain unresolved.

A second, distinct static-struct control adds two static fields and an explicit static constructor to the same eight-byte instance layout. Its first exact Release baseline passed original editor/player behavior but strict player-only recovery emitted only four of six selected methods. Default creation, two null paths and successful forwarding left a separate initialization witness at zero; an explicit static read changed it to one, and later forwarding left it at one. The complete eight-instruction caller contains no initialization call, so the forwarding proof now checks the two instance fields independently of static storage. A separate closed constructor proof binds its complete 106-byte, 19-instruction handler-free body, both TypeInfo metadata initializer calls, once flag, unique witness and owner static fields, ordered increment and stores, and negative-zero bit pattern. It rejects altered native effects, ambiguous bindings, changed field layout or type, and static fields with player custom attributes such as thread-local storage. The generic partial-register lifter remains strict. A fresh original and recovered round trip now passes strict recovery of all six selected methods, pinned typed IL, both zero-difference declaration comparisons over five types, six methods, eleven fields and three attributes, exact Windows editor source compilation and Win64 Release IL2CPP rebuilding. Original and recovered editor/player runs each pass six observations. A source-only recheck after the custom-attribute guard again emits six of six and produces three source/configuration files byte-identical to the accepted native receipt; it does not repeat that build. Other constructor shapes, static initialization schedules and struct layouts remain unsupported.

A bounded reference-null comparison proof now accepts unchanged direct class and array parameters returning managed Boolean results for null and non-null inputs. The three-method neutral fixture passes strict recovery from isolated player binary and metadata only, typed IL verification, and zero-difference declaration comparisons both before and after rebuilding. Fresh generated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as a Windows x64 Release IL2CPP player. Original and recovered editor and player runs each pass five independent observations, including null and non-null class and array inputs, empty and populated arrays, and unchanged neighboring state. This establishes only those proved comparison shapes and tested behavior. A separate terminal bounds-helper classification identifies a proved nonreturning native exit for diagnostics; it does not recover the caller's managed bounds or exception behavior and does not add an accepted method. Paired broad audits through the later native-proof refresh are summarized under Milestone 5.

A closed x64 reference-field store proof now accepts an unchanged class-reference value written through one class-reference owner. It binds the complete null-guarded native body, unique managed method and field metadata, handler-free unwind, proved nonreturning null helper and installed GC write-barrier/card-marker chain before emitting managed `stfld`. The card-marker data proof requires its flag and full table to occupy contiguous readable/writable virtual ranges. The neutral two-method fixture passes strict player-only recovery, typed IL, both zero-difference declaration comparisons, exact Windows editor compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass eight observations, including null owners and values, replacement, aliasing and unchanged neighboring fields. Other write-barrier shapes, volatile stores and exception details remain unresolved.

A separate frame-free x64 byte comparison proof accepts an unchanged ordinary byte field read followed by the exact unsigned threshold at 128. The memory displacement is bounded inside the null page so a null native receiver cannot silently become a successful managed `ldfld`. The two-method fixture passes strict player-only recovery, typed IL, both zero-difference declaration comparisons, exact Windows editor compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass six observations covering both sides of the threshold, byte extremes, unchanged neighboring fields and a null receiver. This does not establish arbitrary byte comparisons or partial-register operations.

The bounded integer-array proof now accepts the exact one-byte read and write shape for `byte[]` and `sbyte[]`, retaining its complete null-first, unsigned-bounds and nonreturning-helper checks. The exact Release player folds both read signatures into one `movzx eax, byte` body and both writes into one byte-store body. Native callers apply signed or unsigned extension to the low-byte return; managed element metadata selects the corresponding typed `ldelem` or `stelem`. A pre-change strict run failed all four methods on unresolved bounds semantics. The corrected four-method player-only recovery passes typed IL, both zero-difference declaration comparisons, exact Windows editor compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass 280 observations covering signed and unsigned boundaries, null and out-of-range failures, aliasing and unchanged array contents. Other one-byte native shapes and array operations remain unresolved. The [Windows x64 return convention](https://learn.microsoft.com/en-us/cpp/build/x64-calling-convention?view=msvc-170) leaves unused return bits undefined; [ECMA-335](https://www.ecma-international.org/wp-content/uploads/ECMA-335_6th_edition_june_2012.pdf) specifies sign or zero extension for small managed array elements.

A separate zero-argument reference-field call control showed that the existing generic recovery path already handles this exact shape. The four selected methods include two implicit constructors, the mutating target and its forwarding call. Strict player-only recovery emitted all four without a new proof; typed IL, both zero-difference declaration comparisons, exact Windows editor compilation and a Release IL2CPP rebuild passed. Original and recovered editor/player runs each pass ten observations for repeated calls, aliasing, signed increment overflow, unchanged neighboring fields and a null receiver. A null-owner observation exercises ordinary caller dispatch. This result validates the controlled shape, not all zero-argument calls or a coverage gain in the broad application scopes.

The scalar-array proof now accepts bounded `float[]` reads and writes with exact four-byte `movss` transfers, unchanged Single metadata, the native float parameter/return registers, null-first and unsigned-bounds branches, and both independently proved nonreturning helpers. The native unwind record includes a final `int3` byte after the bounds call; the proof authenticates that file-backed byte and covers the complete handler-free region while excluding alignment bytes outside it. Before this change, strict player-only recovery rejected both selected methods. The final two-method run passes strict recovery, typed IL verification, both zero-difference declaration comparisons, exact Windows Unity 2021.3.35f1 source compilation and a Windows x64 Release IL2CPP rebuild. Original and recovered editor/player runs each pass 352 bitwise observations covering positive and negative zero, infinities, distinct NaN payloads, subnormals, aliasing, unchanged elements, null arrays and out-of-range indices. This establishes the tested direct Single-array shape; other floating array operations and floating-point status effects remain unverified.

The same closed array proof now accepts two direct 16-bit reads that return `int`: `short[]` uses `movsx eax, word`, while `ushort[]` uses `movzx eax, word`. It requires unchanged signed or unsigned element metadata, a two-byte stride, Int32 index and return, the separate null-first and unsigned-bounds helpers, and the complete trap-terminated handler-free unwind region. It does not admit 16-bit writes. The original exact Windows editor/player each pass 96 observations; pre-change strict player-only recovery rejected both methods with unresolved bounds semantics. The corrected two-method run passes strict recovery, typed IL verification, both zero-difference declaration comparisons, exact Windows Unity source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass all 96 observations, including signed and unsigned extrema, null and out-of-range failures, and unchanged array contents. Other 16-bit native shapes and array operations remain unverified.

The same proof now accepts direct reads from unchanged ordinary class, string and object reference arrays, with the element type identical to the declared return type. It requires an eight-byte element load, pointer-width stride, the complete trap-terminated handler-free native region and both proved nonreturning null and bounds helpers. Reference writes are excluded because covariance and GC write-barrier effects need their own proof. The original exact Windows editor/player each pass 105 identity, covariance, unchanged-array, null-element, null-array and boundary observations; pre-change strict player-only recovery rejected all three selected methods on unresolved bounds semantics. The corrected three-method run recovers typed IL with an array access, passes both zero-difference declaration comparisons, compiles generated source in exact Windows Unity 2021.3.35f1, and rebuilds a Windows x64 Release IL2CPP player. Original and recovered editor/player runs each pass all 105 observations. This controlled result does not establish recovery of longer methods containing reference-array accesses.

A closed x64 Boolean field-getter proof accepts only a complete, file-backed `movzx eax, byte [rcx+field]` leaf ending in `ret`, with no handler, alternate entry or unproved instruction. It binds the raw Boolean return and uniquely located ordinary field to each managed method separately, including when identical native bodies share an address. The field displacement remains inside the null page so a null receiver still faults, and unchanged layout and signature checks reject altered metadata. The neutral four-method fixture includes two implicit constructors and two getters. Strict player-only recovery emits all four selected methods from 11,907 input methods; typed IL and both declaration comparisons pass with zero differences across two types, four methods and four fields. Fresh source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as Windows x64 Release IL2CPP. Original and recovered editor/player runs each pass 14 independent Boolean, neighboring-field and null-receiver observations. Other getter shapes, unavailable modifiers and general Boolean field recovery remain unverified.

A separate eleven-method Boolean-getter metadata control now measures two excluded cases: an own Boolean field below a constructed generic ancestor with inherited native-sized storage, and base/override virtual getters with distinct fields. Its fresh original source compiles in the exact Windows editor and builds a Windows x64 Release IL2CPP player; original editor and player runs each pass eleven observations, including both values, inherited storage, reference aliasing, virtual dispatch and null receivers. The first strict player-only attempt emitted seven implicit constructors and rejected the generic-derived constructor plus all three getters. A bounded getter proof checks the fieldless constructed definition and concrete arguments, walks the unchanged nongeneric ancestor fields and their extents, authenticates virtual slots and override relationships, and isolates a complete five-byte virtual entry from INT3 alignment and a distinct registered unwind function. Focused exact-player mutations reject inherited overlap and a changed virtual slot. That intermediate strict run emitted ten of eleven selected methods.

A separate closed constructor proof binds the remaining complete 57-byte metadata-guarded native body, MethodRef initialization helper, unique immediate constructed base type, and direct tail-call target despite native target aliases. Focused exact-player mutations reject altered native effects and MethodRef/target identity. A fresh player-only run emits all eleven selected methods and passes pinned typed IL verification. Both independent declaration comparisons against the original stripped managed oracle have zero differences across eight types, eleven methods, five fields, three properties and three attributes. The generated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as a Windows x64 Release IL2CPP player with zero build errors. Original and recovered editor/player runs each pass eleven observations covering Boolean values, inherited storage, reference aliasing, virtual dispatch and null receivers. The accepted run used the committed tool on a clean worktree; original managed assemblies were validation oracles after player-only recovery. Other constructed-generic constructor and virtual getter shapes remain unsupported.

The bounded metadata-guarded static-getter proof now also accepts unchanged same-owner `int` and `uint` fields when the complete native body loads the static-fields pointer into `RCX` and one four-byte field value into `EAX`. It requires the exact TypeInfo guard, helper, handler-free unwind, static-field range, raw `I4` or `U4` identity and matching managed return; the existing eight-byte getter path remains separate. The original exact Windows editor/player each pass ten observations, and a pre-change strict player-only run rejected both getters with unresolved guard effects. The corrected two-method run passes strict recovery, typed IL verification, both zero-difference declaration comparisons, exact Windows Unity 2021.3.35f1 source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass all ten observations, including initial defaults, signed and unsigned limits, repeated reads and unchanged neighboring state. Other static initialization, width and field-load shapes remain unverified.

The same closed getter proof now also accepts an unchanged `Boolean` static field only when the complete TypeInfo-guarded body loads the static-fields pointer into `RCX` and zero-extends exactly one byte into `EAX`. It additionally authenticates the installed metadata initializer and rejects output overrides. The enlarged neutral fixture retains the prior pointer and reference getters and adds a neighboring Boolean field. Before the change, strict player-only recovery emitted three of four selected methods and rejected the byte getter at the guard and `movzx`. The corrected four-method run passes strict player-only recovery, typed IL, both zero-difference declaration comparisons, exact Windows Unity 2021.3.35f1 source compilation and a Release IL2CPP rebuild. Original and recovered editor/player runs each pass four independent observations of initial and changed Boolean values, opposite neighboring values after assignment, pointer values and reference identity. Focused exact-player tests reject changed width, signed extension, base/index/destination registers, field/return metadata, marshaling flags, aliases and class initialization. Player metadata may erase an original `volatile` modifier, so this native proof does not certify its absence across other uses; the authored fixture and managed oracle establish this control's declaration facts. Other one-byte static shapes remain unresolved.

A post-commit player-only source recheck again emits all four selected methods. Its five generated source and configuration files are byte-identical to those used by the accepted exact Unity round trip; this recheck did not repeat Unity compilation or behavior.

A separate closed x64 proof now accepts one `int[]` element access through an unchanged instance field. It binds the first owner-field dereference to `ldfld`, then requires the exact array-null and unsigned-bounds branches, one four-byte constant or indexed read/write, both proved nonreturning helpers, and the complete file-backed handler-free unwind region including its final trap byte. The original four-method fixture includes the implicit constructor and passes 110 observations in the exact Windows editor/player. Before this change, strict player-only recovery emitted the constructor but rejected all three accessors on unresolved bounds semantics. The final run recovers all four methods, verifies typed IL and both zero-difference declaration comparisons, compiles generated source in exact Windows Unity 2021.3.35f1, and rebuilds a Release IL2CPP player. Original and recovered editor/player runs each pass 110 observations covering null owners and fields, bounds, signed extremes, successful and failed writes, shared arrays and unchanged neighboring state. Method/type/assembly IL2CPP output options are rejected before replacing native helpers with implicit managed checks, including in the earlier scalar-array proof. Exact-target volatile reference-field evidence includes an extra native barrier that this closed shape rejects, but player metadata alone does not preserve the volatile modifier; other native array shapes and unavailable modifiers remain unresolved.

A closed x64 iterator-factory proof now accepts one handwritten, non-generic `IEnumerator` factory with a complete handler-free native body. It binds the TypeInfo slot, independently authenticates the metadata initializer and allocator chains, proves the integer-state constructor and inert `Object` constructor, and checks the captured-owner field, write barrier and null arm against unchanged managed metadata. Before the change, six of seven selected synthetic methods emitted and the factory failed. Fresh player-only strict recovery now emits all seven with typed IL and zero-difference declaration comparisons before and after rebuilding. The generated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds a Release IL2CPP player; original and recovered editor/player runs each pass 13 observations for fresh instances, state, owner capture, interface methods and null owner. The rebuilt Unity C++ regenerates the TypeInfo guard before allocation. At that checkpoint, a separate compiler-generated iterator control failed strict recovery with `MoveNext` and `Reset` unresolved (six of eight selected emitted); general iterator behavior and exceptional malformed-metadata/resource paths remained unverified.

A second handwritten iterator control compiled by the same exact Windows editor has a 28-instruction, 112-byte factory and a 12-instruction one-argument state constructor. It exercises a separate empty parameterless constructor alias without inferring that alias's source identity from the factory call. The existing bounded proof strictly emits all eight selected methods from isolated player inputs. Pinned ILVerify, both zero-difference declaration comparisons, recovered source compilation and a Win64 Release IL2CPP rebuild pass; original and recovered editor/player runs each pass 13 observations. The separate measured 113-byte factory cluster fails the alternate runtime metadata-initializer proof and also stores zero state before the owner reference, reversing the accepted caller's two field writes. Neither guard was relaxed. This second control is a finite accepted shape, not a recovery claim for that broad cluster.

A third handwritten iterator control exercises a direct call to its own one-argument `Int32` constructor. Its factory has 28 instructions occupying 108 bytes in an exact 109-byte handler-free unwind span with one trap byte; unlike the earlier accepted factory, it has no caller-side state store. The new shape requires the exact instruction sequence, raw executable bytes, TypeInfo, metadata initializer, allocator, null helper, write barrier, owner field and a separately proved 36-byte folded constructor with the unchanged state field. Before this proof, strict player-only recovery emitted seven of eight selected methods; the factory failed. The new path emits all eight, passes pinned typed IL verification and both zero-difference declaration comparisons, and generates source that compiles and rebuilds in the supplied Windows Unity 2021.3.35f1 editor as x64 Release IL2CPP with zero build errors. Original and recovered editor/player runs each pass 21 independent observations for fresh instances, signed state bounds, owner capture, adjacent reference isolation, interface dispatch and null owner. Mutated factory trap bytes, constructor field offsets, calls and metadata reject the proof. Final hardening produced eight source/configuration/report files byte-identical to the accepted Unity run, with typed IL still passing. Allocator exhaustion and other untested exceptional paths remain outside this finite behavior claim.

A separate closed x64 proof accepts one complete literal-concatenation body with an authenticated metadata literal slot, a uniquely compatible inherited getter, an inherited string field, the target runtime null helper and an exact `String.Concat(string, string)` tail call. It checks unchanged signatures/layouts, handler-free unwind, file-backed instructions and the installed runtime helper chain; other native shapes remain rejected. Before the proof, six of seven selected synthetic methods emitted. Fresh strict player-only recovery now emits all seven, verifies typed IL and passes both zero-difference declaration comparisons. Generated source compiles in Windows Unity 2021.3.35f1 and rebuilds as Windows x64 Release IL2CPP; original and recovered editor/player runs each pass ten observations covering repeated literals, nulls, inherited state and virtual dispatch. The complete literal-cache race/resource paths and exception message/stack details are not behaviorally verified, and installed runtime helpers are identified rather than fully lifted.

Shared native code does not identify a managed callee merely because its aliases have the same owner and signature. The resolver no longer chooses an operator or first alias from that evidence alone; an explicit call-site `MethodInfo` can still select its represented member, and receiver type can still select a unique owner. Allocation likewise does not choose between multiple constructor overloads folded to one address. A neutral exact-target control proves the ambiguity: two same-signature, no-inline leaf methods share one player address, while their caller passes a null hidden `MethodInfo`. Its original Windows editor source compilation, Release IL2CPP build, editor behavior and player behavior pass five observations each. Strict player-only recovery emits the two leaves but rejects the caller's unresolved target. This control is a negative safety result, not recovered-source or rebuilt-player acceptance.

A separate exact-target Release by-reference control confirms that an unresolved shared native call is diagnosed before its provisional argument locals are typed. The caller supplies neither a unique managed receiver nor a disambiguating MethodInfo; selecting one of the many methods at that address would invent callee identity. Player-only strict recovery emits one of two selected methods and rejects the caller with an explicit ambiguous-call reason instead of an untyped-local symptom. The original editor/player each pass five observations, but no recovered-source compilation, rebuild or behavioral result is claimed for this rejected control.

The same fail-closed alias rule exposed a previously invalid generated iterator constructor: its raw base-constructor target is shared with a derived `Dispose`, and the earlier emitted IL called `Dispose` before `Object::.ctor`. An exact-profile constructor proof now binds the complete generated constructor body, unchanged state field and independently proved factory to the inert `Object::.ctor` target. A separate closed tail-thunk proof binds empty zero-argument constructors to the unique immediate-base constructor when the entire base chain shares that thunk and the terminal Object constructor is authenticated. A bounded generated `MoveNext` proof preserves its state write before a captured-owner null failure, current-field assignment, write barrier and state-dependent returns. Five focused exact-player proof tests with negative mutations pass. At that checkpoint, the compiler-generated iterator advanced from six to seven of eight selected methods under player-only recovery; `Reset` failed because its MethodDef initializer path and terminal exception behavior had not yet been fully authenticated. The partial assembly's managed IL verifies, including valid diagnostic fallback IL for `Reset`, but that fallback is not recovered behavior. There is no recovered-source Unity compilation, rebuilt player or behavioral acceptance for this eight-method fixture. Strict rechecks of the previously accepted manual iterator and literal-concatenation fixtures remain seven of seven with valid managed IL; their generated source/configuration files are byte-identical to the accepted exact Unity round trips, so no new Unity rebuild was needed for those unchanged outputs.

The terminal managed-throw proof now bounds the caller to its authenticated 70-byte unwind function when a metadata-derived method span continues into later native code. It retains the exact caller shape, file bytes, handler-free unwind, trap padding, method identity, TypeInfo and MethodDef slots, allocator, null helper, exception constructor and nonreturning raise wrapper. Focused exact-player tests pass for both the original 70-byte throw control and the compiler-generated iterator `Reset`; altered or omitted instructions within the unwind function are rejected, while changes beyond it are ignored. The current iterator player-only run emits all eight selected method bodies and passes pinned typed IL verification. After the indexed-Type emission gate, a fresh strict player-only Unity-source export exits successfully with all eight selected methods emitted, no source diagnostics, and `DECL001`/`DECL004` preserving unresolved return-row and raw Type-name-qualification facts. Its source and assembly configuration match the earlier partial candidate byte-for-byte, but that candidate's diagnostic `Reset` fallback was not recovered behavior; the native throw proof now supplies it. The regenerated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds a Windows x64 Release IL2CPP player. A freshly rebuilt authored control and the regenerated project each pass 16 editor and 16 player observations for fresh enumerators, deferred owner reads, completion, disposal and `Reset`; their observation arrays match. The recovered managed and rebuilt stripped assemblies each have zero declaration-projection differences from the independent original stripped managed oracle (two types, eight methods, four fields, two properties and ten attributes), with no stripping losses or comparer diagnostics. The `IteratorStateMachineAttribute` serialized Type value has the same unqualified nested type name in the original stripped, original unstripped, recovered managed and rebuilt stripped assemblies. That exact fixture agreement does not recover raw qualification generally from player metadata; `DECL001` and `DECL004` remain open for player-only declaration fidelity. The finite behavior probe does not prove all exceptional paths or general compiler-generated iterator recovery.

Failure triage found a recurring runtime metadata guard with one register move between a RIP-relative byte comparison and its branch in 484 and 815 selected failed methods of the two corpora. These counts are upper bounds for a native shape, not recovered methods. A neutral exact-target three-method fixture supplies two controlled instances: one saves an integer argument in EBX, and one saves an object receiver in RBX. Both guard an authenticated TypeInfo slot with the installed metadata initializer, branch past the matching flag store, and preserve the moved value for later use. Original source compiles in Windows Unity 2021.3.35f1 and builds as Windows x64 Release IL2CPP; original editor/player runs each pass six observations covering first use, changed static state, null receiver and signed overflow. Its baseline strict player-only recovery emitted only the fixture constructor; the two guarded methods failed on unsupported flag and null-guard semantics. This baseline had no recovered-source compilation, rebuilt player or behavioral acceptance.

The exact-profile guard classifier recognizes a single flag-preserving register move between the comparison and branch only when the argument slot is independently identified as runtime metadata. It retains the move and all guard/helper instructions for nonliteral metadata; it does not turn the guard into recovered behavior. Focused tests match both fixture TypeInfo guards and reject altered flags, memory operations, partial registers, branch targets, helper targets and flag stores. At that classifier checkpoint, a strict player-only run still emitted one of the three fixture methods and rejected the two guarded methods. One failure named the unresolved metadata initializer explicitly; the other remained an unresolved native null guard. That checkpoint added no generated-source, rebuilt-player or behavioral acceptance.

A separate closed x64 proof accepts one complete TypeInfo-guarded static `Int32` field read plus unchanged `Int32` parameter addition. It authenticates the metadata initializer, unique method binding, owner/field/parameter identity and layout, complete handler-free unwind/body, writable metadata slot and zero-filled flag, exact branch and arithmetic data flow, and absence of a managed class constructor. The neutral single-method fixture passes strict player-only recovery (one of one), typed IL, both zero-difference declaration comparisons, exact Windows editor source compilation and a Windows x64 Release IL2CPP rebuild. Original and recovered editor/player runs each pass six independent observations covering first use, changed static state, unchanged neighboring state and signed overflow. Focused exact-player mutations reject aliasing and altered native/metadata shapes. At that checkpoint, the separate three-method moved-guard fixture emitted two of three methods and strict output rejected its object-receiver method. The accepted arithmetic slice does not establish general metadata initialization, class-constructor, concurrency or exceptional resource behavior.

A post-commit strict source-only recheck of the accepted single-method slice again emits one of one with no source diagnostics; six generated source/configuration files are byte-identical to those used by the passed exact Unity round trip. This comparison adds no new Unity build or behavioral run.

A second closed x64 proof now accepts a complete TypeInfo-guarded static `Int32` plus an `Int32` instance field of a class parameter. It authenticates the terminal no-return null helper and exclusive null branch, unique method/field bindings, exact 18-instruction body and handler-free unwind, metadata helper/slot/flag, object and static field layouts, and no managed class constructor on the static owner. Direct IL retains the static-field read, object-field dereference and unchecked addition. Native and metadata mutation tests reject altered branches, widths, offsets, aliases and initialization effects. Fresh strict player-only recovery of the three-method moved-guard fixture emits all three with valid typed IL and zero source diagnostics; both declaration comparisons have zero differences against the original stripped managed oracle. Original and recovered source compile in the supplied Windows editor and build Windows x64 Release IL2CPP players. Each original and recovered editor/player run passes six independent observations including a null receiver, changed static state and signed overflow. This proves the controlled null/field path, not general runtime metadata initialization, thread interleavings or exact exception details.

A post-commit strict source-only recheck again emits all three moved-guard methods; six generated source/configuration files are byte-identical to those used by the accepted exact Unity rebuild. This recheck did not repeat Unity compilation or behavior.

A separate frame-free x64 proof now accepts a complete branch on the low byte of an unchanged second Boolean argument. It requires a closed `test dl, dl` and conditional/tail-jump shape, a zeroed hidden argument for each tail target, uniquely bound executable no-argument static `Int32` callees in the same owner, no owner class constructor, executable file-backed padding and no native unwind entry. Focused exact-player mutations reject changed flags, argument metadata, target bindings, entry boundaries and initialization effects. A neutral three-method fixture passes strict player-only recovery with no unresolved selected methods, typed IL verification and both zero-difference declaration comparisons. Original and regenerated source compile in Windows Unity 2021.3.35f1 and build Windows x64 Release IL2CPP players; each original and recovered editor/player run passes 14 independent observations across both branch outcomes and unused first-argument values. This establishes the bounded tail branch, not arbitrary partial-register operations or other tail-call shapes.

A post-commit strict player-only recheck of the Boolean branch fixture again emits all three selected methods; all five generated source/configuration files are byte-identical to the accepted exact Unity round trip. It did not repeat Unity compilation or behavior.

A new neutral three-method control isolates two sequential reference null guards and a separate one-guard integer-addition path. Its original source compiles in the supplied Windows Unity 2021.3.35f1 editor and builds a Windows x64 Release IL2CPP player; original editor and player each pass 19 observations for both null positions, both null, signed limits, aliasing, overflow and unchanged neighboring fields. The native comparison checks the two receivers in order, sends both null edges to one terminal helper, and then reads their `Int32` fields. Initial strict player-only recovery emits the implicit constructor, rejects the comparison at an uncoalesced runtime null guard and rejects the arithmetic control because an intermediate integer local is untyped. This baseline has no recovered-source Unity compilation, native rebuild or behavioral acceptance.

The bounded fix carries 32/64-bit native Add destination width into ISIL and infers signed integer result types only from compatible typed operands or representable immediates; the emitter rejects width conflicts. Eighteen focused arithmetic tests pass. A separate complete 18-instruction x64 body proof binds both ordered null checks, field loads, helper target, unwind and file-backed bytes before a null-arm field probe can preserve the first failure. Thirteen focused native and graph checks pass. The recovered three-method result and exact-target round trip are recorded under Milestone 4.

A neutral class-cast and literal-concatenation fixture exercises four explicit behaviors in one assembly. Its 20-instruction combined caller matches a measured broad-audit candidate, while its 37-instruction class-cast lookup is a one-instruction register-allocation variant of a separate 38-instruction candidate. Initial strict player-only recovery emitted six implicit constructors and rejected all four explicit methods. The bounded cast, static-literal, class-cast-callee and type-from-handle proofs now emit all ten selected methods from isolated player inputs. Pinned ILVerify, both zero-difference declaration comparisons, recovered source compilation in the supplied Windows editor, and a Win64 Release IL2CPP rebuild pass. Original and rebuilt editor/player runs each pass 23 independent observations for null and incompatible casts, exact/subclass identity, aliases, type tokens, literals, virtual calling and unchanged neighboring fields. The class-cast-callee route rejects inaccessible or hidden inherited methods; the type-token route retains the `System.Type` class-init call. The 38-instruction broad-audit lookup still fails its alternate metadata initializer proof, and this finite fixture does not establish arbitrary class casts, metadata initialization or whole-project behavior.

Two smaller fixtures isolate its cast and static-literal paths. A closed 37/38-instruction class-cast proof binds the target TypeInfo, field and class hierarchy offsets, both ordered native exits, unwind and initial once flag; a file-backed zero flag is accepted only with a complete relocation-overlap check. The cast-only fixture now passes strict player-only recovery of all five selected methods, pinned ILVerify, zero differences in both declaration comparisons, recovered source compilation in the supplied Windows editor and a Win64 Release IL2CPP rebuild. Original and rebuilt editor/player runs each pass 12 observations for null/incompatible/exact/subclass references, identity, aliases and unchanged fields. The initial strict failure from the file-backed flag remains a negative receipt. The 38-instruction broad-audit lookups still fail their alternate metadata initializer proof.

A separate closed 14-instruction static-literal proof binds the guarded StringLiteral slot, authenticated initializer arm and unique `String.Concat(string,string)` tail, while rejecting class-initializer and nondefault-layout owners. The literal-only fixture passes strict player-only recovery of its one selected method, pinned ILVerify, both zero-difference declaration comparisons, exact Windows editor compilation and Win64 Release IL2CPP rebuilding. Original and rebuilt editor/player runs each pass six observations for null, empty, repeated and Unicode inputs, result identity and an unchanged neighboring field. These isolated results do not establish arbitrary class casts or general metadata initialization; the combined caller has its own acceptance above.

A neutral two-assembly control isolates a virtual string getter, its 37-instruction class-cast helper and a guarded derived constructor. The source-field type and two ancestor static constructors are in an explicit synthetic embedded package; the cast target and caller remain in the selected assembly. The cast proof permits an unchanged direct assembly reference for the source type and explicit static constructors only on non-target, non-source ancestors; it still rejects a target static constructor and private inherited source fields. A separate 73-byte, 17-instruction constructor proof authenticates the immediate-base TypeInfo guard, class-initialization status and export, exact once flags, a two-alias folded base-constructor body and its inert terminal Object thunk before emitting the managed base call. Focused exact-player tests reject changed native branches, targets, metadata, assembly references and alias sets. Strict recovery from the player binary and metadata emits all five selected methods with zero unresolved, versus two before these proofs. Source reference closure uses the package's independently recorded managed DLL and an explicit assembly-reference map; this is an auxiliary-assisted source round trip, not a claim that the package was recovered from player inputs. Pinned ILVerify and both declaration comparisons against the stripped fixture oracle pass with zero differences across three types, five methods, three fields, one property, three attributes and two assembly references. The generated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds a Win64 Release IL2CPP player. Original and recovered editor/player runs each pass ten observations, including cold and repeated construction, ancestor initializer order, null and incompatible casts, virtual dispatch and unchanged fields. Version-29 player-only return-row facts remain unknown despite the zero-difference oracle comparisons. A retained seven-method variant with one ancestor static constructor inside the selected assembly remains five of seven strict; its static constructor and derived constructor have no accepted recovery. A separate original-only throwing-initializer control observes cached `TypeInitializationException` before outer initialization, but its third constructor alias falls outside this proof and it has no recovered behavior result. The broad diagnostic disposition gain is recorded under Milestone 5; arbitrary class casts, folded constructors and whole-project behavior remain unverified.

A separate neutral terminal-throw control isolates two 70-byte, 17-instruction exception bodies and a returning-call negative control. The bounded proof authenticates allocation, a null-helper path, the TypeInfo and MethodDef metadata routes, the exact exception constructor, unwind and PE bytes, and a terminal wrapper tied to the exported runtime raise entry; an arbitrary final call cannot authorize a throw. The MethodDef proof checks the complete bounded definition resolver and entry prefixes for its transitive runtime callees, without proving their complete bodies. The throw-only fixture now passes strict player-only recovery of all five selected methods, pinned ILVerify, zero differences at both declaration comparisons, recovered compilation in the supplied Windows editor and a Win64 Release IL2CPP rebuild. Original and recovered editor/player runs each pass nine observations for exact exception types, null owners, signed wraparound, post-call effects and unchanged neighboring fields. After tightening the MethodDef route, a later player-only recheck retains five of five strict methods and byte-identical generated C# and Unity configuration; that recheck did not repeat compilation, rebuilding or behavior. Stack-trace text and broader exception semantics remain unverified. The initial three-of-five strict failure remains a negative receipt. Two broad audits contain 26 and 293 distinct selected methods with the same measured caller shape, but their alternate metadata initializer fails the current helper proof and their Release build configuration is unverified. No broad typed-IL, Unity build or behavioral result is claimed for those methods.

A neutral explicit-interface control measures two constructed generic calls through an external synthetic package. Both wrapper bodies are complete file-backed, handler-free 62-byte/14-instruction functions with distinct initialized MethodRef slots. Their shared native tail has three managed aliases: the generic method definition, a same-name nongeneric overload and one concrete generic instance. A bounded proof binds each wrapper's MethodRef usage and exact generic target, signed argument, metadata initialization and relocation-free slot; it rejects a target class constructor and ambiguous call identity. Strict player-only recovery emits three of four selected methods, with the abstract interface method correctly classified as having no managed body and zero unresolved. Pinned IL verification and both original-oracle declaration comparisons pass with zero differences. Original and regenerated source compile in the supplied Windows Unity 2021.3.35f1 editor and build Windows x64 Release IL2CPP players with zero errors; both editor and player stages pass nine independent observations on each side for explicit dispatch, signed keys, per-instance effects, aliases and null calls. Post-hardening strict source and configuration are byte-identical to the accepted exact-Unity run, and pinned IL verification passes again. The exact two-alias broad diagnostic shape is not reproduced, and the package source, reference map and compiled package assembly are disclosed auxiliary inputs for source reference closure, separate from player-only body recovery. Other generic dispatch and shared-address shapes remain unsupported.

A second neutral control forwards an `Exception` argument and one of two literals from instance wrappers to an external static sink with an explicit class constructor. Both wrapper bodies have the same complete 93-byte/20-instruction handler-free native shape as a 19-method selected diagnostic cohort. One wrapper's reported raw span continues through trap padding into a later unrelated function, so the proof binds its semantic boundary to the exact unwind entry and file-backed bytes. It also proves the once flag, relocation-free TypeInfo and StringLiteral slots, metadata helpers, class-init guard and unique sink target. Strict player-only recovery emits all three selected methods with zero unresolved; pinned IL verification and both original-oracle declaration comparisons pass with zero differences. Original and regenerated source compile in the supplied Windows Unity 2021.3.35f1 editor and build Windows x64 Release IL2CPP players with zero errors. Original and recovered editor/player runs each pass nine observations for cold initialization order, repeated calls, exact exception reference including null, receiver aliases and unchanged neighboring fields. Post-hardening strict source and configuration are byte-identical to the accepted Unity run, with pinned IL verification repeated. The broad diagnostic cohort's original build is not a clean baseline and supplies no behavioral acceptance for those 19 methods. Other class-initialization and external-call shapes remain unsupported.

An exact-target constructor control has two sealed direct `Object` children whose `Int32` field-initializing constructors fold to one native address with nine managed aliases. The authoritative handler-free unwind body is 36 bytes and 12 instructions; the metadata-derived raw span continues for 252 bytes into an unrelated later function. A bounded proof verifies the frame, each file-backed executable byte, trap padding and the next independent unwind entry, then binds the inert `Object` base constructor, incoming signed argument and unchanged field layout. Strict player-only recovery emits both selected constructors without unresolved behavior; pinned IL verification and both declaration comparisons pass with zero differences. Original and regenerated source compile in the supplied Windows Unity 2021.3.35f1 editor and build Windows x64 Release IL2CPP players with zero errors. Original and recovered editor/player runs each pass eight observations for signed boundaries, reflection construction, fresh instances, aliases and neighboring-field preservation. Subsequent decoded-byte hardening leaves generated source and configuration byte-identical to the Unity-accepted run and passes pinned IL verification again. The proved native `Object` constructor target has many managed aliases in this exact fixture; its inert effect is authenticated, but the original folded callsite MethodDef identity remains unavailable. Similar broad-player constructor shapes remain without whole-scope source compilation, rebuilding or behavioral validation.

Prioritize measured failure categories rather than adding broad pattern collections without evidence.

| Feature group | Required focus |
| --- | --- |
| ABI and calls | Argument registers/stack, shadow space, hidden method/context arguments, return buffers, struct passing/returns, thunks and indirect calls |
| Control/data flow | Flags, signed/unsigned comparisons, integer widths, joins/phi values, loops, switches, aliasing, tail calls and optimized/inlined code |
| Object/runtime behavior | Allocation, constructors, class initialization, null/bounds checks, boxing/unboxing, casts, field access and runtime helper effects |
| Arrays and values | Element layout, by-reference access, value-type copies, floating-point edge cases and vector instructions as encountered |
| Generics and dispatch | RGCTX, generic sharing, inflated signatures/layouts, virtual/interface calls, delegates and shared native bodies |
| Exceptions | Throw/rethrow, catch/finally/filter regions and cleanup; missing recovery must stay explicit |
| Generated constructs | Closures, iterators/coroutines and async state machines, preserving behavior even when original syntax is unavailable |

Use Binary Ninja when native ambiguity needs investigation. Generalize each supported pattern from controlled fixtures and validate its preconditions. Remove runtime helpers only when their managed effects are preserved; do not erase initialization or exception behavior to simplify output.

Exit evidence per feature: a targeted regression where needed, valid output, exact-editor compilation, and behavioral comparisons including meaningful boundary cases. Unsupported cases continue to fail strict validation with actionable reasons.

## Milestone 4 — Unity project and build verification

Status: in progress. Complete native round trips pass for arithmetic, unsigned comparisons, scalar structs, bounded static-struct initialization and forwarding, right shifts, guarded division, metadata literals, bounded guarded static literal concatenation, class-cast lookup, type-from-handle and composed cast/literal calls, and terminal managed throws, fresh component instances, scalar floating comparisons, ordinary byte/word-field zero comparisons, integer extensions, scalar binary64 truncation, bounded direct-call loops with null receivers, bounded zero-argument reference-field calls, bounded array-returning instance calls, bounded direct, `this` and one-level nested string/object/`int[]`/`string[]`/`object[]` field reads, bounded direct application-class field reads, derived owners, neighboring array fields and exact shared native bodies, shared empty constructor chains and one bounded constructed-generic base constructor, bounded `int[]`, `uint[]`, `long[]`, `ulong[]`, `byte[]` and `sbyte[]` reads and writes, bounded direct class/string/object array reads, bounded signed 32/64-bit instance-field reads and writes, bounded same-owner static getters, bounded Boolean-return call branches and virtual Boolean field getters, bounded direct Boolean field getters, bounded Boolean field reads through first or second argument registers, Boolean, direct signed 32-bit and nested signed 32-bit zero stores, including a large-offset nested Boolean-store control, and the closed three-field scalar-float reference-mutation control. The runner requires independent declaration comparisons before and after rebuilding; the loop-call, array-access and field-access runs have zero differences at both gates. Original managed oracles are consulted only after player-only recovery. The bounded virtual string getter and guarded constructor chain now also pass an exact native round trip with a disclosed synthetic auxiliary assembly for source reference closure. Broader assembly/project configuration and script discovery/bindings remain active work. Original script GUIDs and asset bindings are not reconstructed by the current code-only exporter.

The generic-interface wrapper, guarded external sink call, folded field-initializing constructor, direct-constructor iterator factory and static `Int32` setter controls above also pass strict player-only recovery, typed IL, both declaration comparisons, exact Windows editor compilation, Release IL2CPP native rebuilding and finite original/recovered editor and player observations. Their accepted method counts are three emitted plus one abstract method without a body, three emitted, two emitted, eight emitted and one emitted, respectively. The external synthetic packages used by the first two are disclosed source-reference inputs, separate from body recovery. These finite controls do not establish broader application/project acceptance.

A later source-only parity check keeps the accepted arithmetic, reference-store and static Boolean getter selections and their generated source/configuration files byte-identical. The field-backed array constructor had then become unresolved after the ambiguous native-alias correction at `5d7740a6`: its folded tail has multiple compatible native target aliases and no MethodInfo evidence identifying the original native callee. The bounded constructor proof now isolates its reachable seven-byte tail from a longer reported method span, authenticates contiguous trap padding and the next handler-free unwind entry, and requires a single unchanged parameterless constructor directly under `System.Object`. [C# constructor rules](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/language-specification/classes#15112-constructor-initializers) require its direct base initializer; the proof emits that canonical inert managed call without choosing a native alias as the original MethodDef identity. Original initializer syntax and optimized-away calls remain unavailable. A fresh strict player-only run emits all four selected methods, verifies typed IL, and passes two zero-difference declaration comparisons. Its source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds a Windows x64 Release IL2CPP player with zero build errors. Original and recovered editor/player stages each pass 110 observations. After tightening the padding and unwind checks, a second strict player-only export retains four of four emitted and produces all four source/configuration files byte-identically; pinned IL verification passes. That final check did not repeat Unity compilation or behavior.

The separate three-method sequential-null-guard control passes strict player-only recovery and typed IL, with zero differences at both declaration gates. Its generated source compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as a Windows x64 Release IL2CPP player. Original and recovered editor/player runs each pass nineteen observations, including ordered null failures and signed 32-bit addition. The first null arm is represented by a field read only when a complete file-backed native body, exact unwind and helper target bind that read to the same unchanged field and receiver. A post-commit strict player-only recheck again emits all three methods with no source diagnostics; all six generated source/configuration files are byte-identical to the accepted round-trip output. That recheck did not repeat Unity compilation, rebuilding or behavior. Other shared-helper shapes remain unresolved.

The separate seven-method inherited string-literal control passes exact Windows editor compilation and a Release IL2CPP rebuild with zero errors. A strict player-only source recheck after final helper hardening emits all seven methods and produces six source/project configuration files byte-identical to those used by the accepted native round trip; this recheck did not repeat the Unity build.

The single-method TypeInfo-guarded static addition control also passes a fresh original and recovered Windows x64 Release IL2CPP round trip, with zero-difference declaration comparisons before and after rebuilding. Its original and regenerated source each compile in the exact editor, and both editor/player builds pass six independent observations.

A separate static `Int32` setter control has a complete 59-byte, 14-instruction handler-free native body. The bounded proof authenticates the TypeInfo once guard and installed initializer, preserves the input through RBX, binds the own four-byte static-field store and rejects a managed class constructor, readonly target, overlap, native alias or changed output signature. Its preproof strict run rejected the sole selected method; strict player-only recovery now emits it. Pinned IL verification and both zero-difference declaration comparisons pass. The generated source compiles and rebuilds in the supplied Windows Unity 2021.3.35f1 editor as x64 Release IL2CPP with zero errors; original and recovered editor/player runs each pass eight observations across repeated writes, signed limits and neighboring fields. After the readonly guard was added, eight source/configuration/report files remained byte-identical to the accepted Unity run and typed IL still passed. Metadata-initializer resource failures and untested exception paths are outside this finite behavior result.

A separate instance-reference-setter fixture has two ordinary classes with setter-only object and string properties, two implicit constructors, and 25 independent observations. Its exact original Windows x64 Release IL2CPP player folds both setters onto one 12-byte leaf that stores the argument through the receiver and tail-jumps through two direct thunks to the installed GC card marker. The method-size estimate extends into a separate function; the proof authenticates the reachable bytes, trap padding, next unwind entry, write-barrier tail, each unchanged setter/property/field binding, and same-assembly aliases. Before the proof, strict player-only recovery emitted the constructors and rejected both setters. A fresh strict run now emits all four selected methods, passes typed IL and zero-difference declaration comparisons before and after rebuilding, and compiles clean generated source in the supplied Windows Unity 2021.3.35f1 editor. The recovered Windows x64 Release IL2CPP build succeeds with zero errors; original and recovered editor/player runs each pass all 25 observations, including identity, replacement, neighboring fields, aliasing and null receivers. Other native store shapes and concurrent or volatile declaration semantics remain unverified.

A separate read/write reference-property fixture adds an ordinary object getter to one cell while retaining its setter and a second setter-only string cell. The exact original player folds both setters onto the same proved 12-byte store and write-barrier tail as the earlier setter-only fixture. Before widening the metadata gate, strict player-only recovery emitted the two constructors and getter but rejected both setters: the read/write setter failed the getter-presence check, which also blocked its folded setter-only alias. The bounded proof now binds each setter to its own unchanged property and field even when a getter exists; the getter is recovered separately. Fresh strict player-only recovery emits all five selected methods, typed IL verifies, and both independent declaration comparisons have zero differences and no stripping losses across two types, two properties, six fields, five methods and two parameters. Generated C# compiles in the supplied Windows Unity 2021.3.35f1 editor and rebuilds as Windows x64 Release IL2CPP with zero errors. Original and recovered editor/player runs each pass all 34 observations covering getter identity, replacement, null clearing, reflected property accessors, neighboring state and null receivers. Version-29 player-only return-row provenance remains unresolved, and other native stores, volatile declarations and concurrent behavior are unverified.

The full three-method moved-guard control now also passes a fresh player-only round trip after a separate bounded object-field/null-path proof. It passes typed IL, both zero-difference declaration comparisons, exact Windows source compilation, a Release IL2CPP rebuild, and six matching editor/player observations at each original and recovered stage. The prior two-of-three strict rejection remains a historical checkpoint.
The three-method second-argument Boolean branch control also passes a fresh exact-target native round trip. Its generated source calls the two independently recovered helpers on the verified branch paths, with zero declaration differences before and after rebuilding and 14 matching original/recovered editor/player observations at each stage.
The expanded four-method static-getter control passes a fresh exact-target round trip after adding the closed Boolean byte-load proof. It passes typed IL, both zero-difference declaration gates, Windows source compilation and Release IL2CPP rebuilding, with four matching editor/player observations at each original and recovered stage.

A separate emitter-only component probe now passes in the supplied Windows editor with `NET_Unity_4_8`: two component scripts map to their expected classes and fresh-instance script assets, all six serialized field paths/types/kinds match, and the ordinary helper and assembly attribute survive. The previous single-file output compiled but discovered zero of the two scripts. Deterministic class-matching files, safe namespace/assembly directories and explicit name/collision diagnostics fix that measured defect. The source report mappings also match the editor's observed paths. This probe deliberately uses an authored managed oracle; it does not establish player-only body recovery or restoration of original GUIDs/scenes.

The later three-body component round trip recovers from isolated player inputs, passes typed IL verification and both declaration comparisons with zero differences, and rebuilds/runs in the exact Windows editor. Two fresh instance pairs retain six field paths before and after assignment, including private strings, a nested value field and object-reference identity. Source/declaration resolution uses complete assembly identities across explicit framework versions; ILVerify uses a separately recorded reference set without duplicate simple names. The same player-only generated source also passes a fresh exact-Windows-editor discovery probe: both component scripts bind to their expected classes and fresh instances, and all six serialized field paths/types/kinds match. The probe authenticates all five copied source/configuration files against the passed recovery receipt both before and after the editor run. These runtime observations do not reconstruct serialized scenes or original script GUIDs.

An explicit auxiliary Unity package manifest now passes bounded validation and is copied without inferring package identities from the player. A fresh three-method reference-null round trip with one supplied built-in module dependency preserved the manifest bytes and resolved package lock across the original and recovered projects. It passed strict player-only body recovery, typed IL, both zero-difference declaration comparisons, exact-editor compilation, a Windows x64 Release IL2CPP rebuild and five matching observations in each original/recovered editor and player run. This proves the controlled manifest path.

An explicit external-reference map now distinguishes Unity assembly-definition references from precompiled plug-ins and target-provided assemblies without inferring that distinction from player metadata. Unknown kinds produce `SOURCE007` and partial output; strict recovery rejects them. A synthetic one-method fixture with an embedded package assembly and a separately compiled managed plug-in passes strict recovery with disclosed auxiliary map/dependency assemblies, typed IL, both zero-difference declaration gates, fresh source compilation and a Windows x64 Release IL2CPP rebuild. Original and recovered editor/player runs each pass four observations; the runner authenticates each dependency build, source input, SDK and embedded package resolution. This is an auxiliary-input result, not player-only dependency discovery. A controlled plug-in whose file name differed from its managed assembly identity compiled in the editor but failed the exact Windows IL2CPP native build during assembly resolution, so that layout is unsupported in the verified path. Other package layouts, plug-in importer settings and project-wide dependency closure remain unverified.

An exact-target framework-reference fixture retains one `System.Numerics.BigInteger` field and one authored arithmetic method. Before the change, strict player-only source output emitted the method but rejected the known framework reference as unclassified (`SOURCE007`); a separately disclosed explicit map passed the complete round trip. The exporter now recognizes only the exact `System.Numerics` assembly identity supplied by the Unity 2021.3.35f1 `NET_Unity_4_8` profile; a different version or public-key token remains unclassified. A fresh no-map player-only run recovers its one selected method, passes typed IL and both zero-difference declaration comparisons, compiles generated source in the supplied Windows editor and rebuilds a Release IL2CPP player. Original and recovered editor/player runs each pass four observations, including reflection of the framework field identity. A final strict source-only rerun after a report-provenance correction produces byte-identical C# and asmdef files to the passed native receipt. This verifies one target-provided reference, not general project dependency discovery.

The Unity target-assembly classification now uses exact names and managed identities inventoried from all 81 module DLLs plus the `UnityEngine` and `UnityEditor` base assemblies in the supplied Windows 2021.3.35f1 editor: version `0.0.0.0`, neutral culture and no public-key token. An unknown module name or mismatched version/token is unclassified; an explicit target-provided map cannot override an unknown or mismatched target identity. Five synthetic cases cover those boundaries. A strict player-only source recheck on retained component fixture inputs emitted all three selected methods with no diagnostics and classified `UnityEngine.CoreModule` by target identity; all eight generated source/configuration files were byte-identical to that fixture's previously passed native round-trip receipt. This recheck did not launch Unity or add a new compilation, native-build or behavioral result. Package and plug-in identities remain explicit auxiliary dependencies.

The exporter now reports `SOURCE009` and partial output when a selected predefined Editor assembly or an assembly referencing `UnityEditor` would be emitted without an Editor-only platform restriction. Three synthetic cases check both predefined assembly names and a custom Editor reference. Strict output rejects this unresolved project layout; no additional player method, exact-editor compilation or native rebuild is claimed. Unity 2021.3 [requires Editor-only assembly definitions to include only the Editor platform](https://docs.unity3d.com/2021.3/Documentation/Manual/ScriptCompilationAssemblyDefinitionFiles.html). Such assemblies are normally absent from player-only IL2CPP inputs.

- Expand the minimal exporter to a deterministic application-source/project layout, assembly references, packages, platform defines and required helpers. Identify dependencies that player metadata cannot reconstruct and require explicit local configuration for them.
- Compile in a fresh project with the supplied Unity 2021.3.35f1 editor. Use its documented [batch mode and editor entry-point arguments](https://docs.unity3d.com/2021.3/Documentation/Manual/EditorCommandLineArguments.html); validate completion and generated outputs as well as process status. Do not suppress compiler errors or use stale assemblies to pass.
- Build the Windows x64 player with IL2CPP and native `Release` settings explicitly selected. Record Development Build, stripping, code-generation and toolchain settings; run the built fixture in the supported local environment.
- Compare original and rebuilt behavior with deterministic inputs. Record platform/timing-sensitive cases and mismatches rather than claiming equivalence from a successful launch.
- Verify MonoBehaviour/ScriptableObject identity, serialized fields and script bindings where supporting asset data is available. Treat missing asset/project information as a separate limitation, not permission to invent original GUIDs or scene data.

Exit evidence: a clean regeneration/import/build/run cycle with reproducible settings, no compiler errors, and no unreported fallback or excluded method in the declared verified scope. macOS import checks and Windows player results remain separately labeled.

## Milestone 5 — Private integration and maintainable coverage

Status: baselines measured; integration acceptance remains incomplete. Every tabled run retains all 7,256 selected private methods and all 12,724 selected independent-application methods. The table is a historical sequence of analysis dispositions through the shared terminal runtime null-helper checkpoint; later measured changes appear in the prose above and have not been folded into this table. Both complete scopes fail strict recovery in every tabled run. Neither corpus has an accepted source project, typed-IL result or behavioral equivalence result.

A paired player-only audit of the authenticated iterator-factory proof retains all 38,700 and 91,198 full-input method identities and the 7,256 and 12,724 selected denominators. The selected scopes remain 1,731 emitted / 4,506 failed / 2 partial / 1,017 without managed bodies and 4,606 emitted / 7,558 failed / 81 partial / 479 without managed bodies. Both full reports are byte-identical to their respective pre-proof reports, with zero selected or full-input disposition transitions; both strict runs still reject. The separately observed alternative factory cluster has 178 candidates, but their native shapes are outside the accepted proof. This audit adds no broad Unity compilation, native rebuild or behavioral evidence.

The paired authenticated player-only audit after the literal-concatenation proof again retains all 38,700 and 91,198 ordered full-input identities and both selected denominators. Both complete reports are byte-identical to the prior iterator-factory snapshot, with zero selected or full-input disposition transitions. Selected counts remain 1,731 emitted / 4,506 failed / 2 partial / 1,017 without managed bodies and 4,606 emitted / 7,558 failed / 81 partial / 479 without managed bodies. Both strict scopes still reject; neither has whole-scope typed IL, Unity compilation, native rebuilding or behavioral-equivalence evidence from this audit. The seven-method exact-target fixture supplies the bounded acceptance result.

Removing the same-owner/same-signature alias guess reclassifies nine selected emitted methods as failed in the first player-only corpus and thirteen in the second; one further selected partial method becomes failed in the second. The resulting selected counts are 1,722 emitted / 4,515 failed / 2 partial / 1,017 without managed bodies and 4,593 emitted / 7,572 failed / 80 partial / 479 without managed bodies. Across the full input identities, eleven emitted methods become failed in the first corpus and eighteen emitted plus one partial become failed in the second. Both strict scopes still reject. These transitions expose previously unsupported call identities; no broad Unity compilation, native rebuilding or behavioral result is claimed. The audit snapshot predates the separate constructor-overload guard, which has a focused synthetic regression but no paired broad measurement yet.

A fresh paired audit of the committed constructor-alias guard and closed iterator/empty-constructor proofs preserves all 38,700 and 91,198 ordered full-input identities and both selected denominators. Relative to the preceding alias audit, 68 full-input emitted constructors become failed in the first corpus, including 29 selected; 600 become failed in the second, including 389 selected. Every disposition transition in this pair is a constructor Emitted-to-Failed transition. Selected counts are now 1,693 emitted / 4,544 failed / 2 partial / 1,017 without managed bodies and 4,204 emitted / 7,961 failed / 80 partial / 479 without managed bodies. The guard declines to identify a base-constructor call solely from a typed `this` receiver when the native address has another method alias; these reports do not establish whether the previously emitted constructors had valid managed IL or correct behavior. Both broad strict scopes still reject and have no whole-scope typed IL, Unity compilation, native rebuild or behavioral-equivalence evidence. Exact rechecks of the accepted manual iterator and literal-concatenation controls remain seven of seven with byte-identical generated source/configuration.

Read-only replay of the pinned pre-guard tool over the 418 selected emitted-to-failed constructor transitions found 358 old IL bodies without a call to the immediate-base constructor on `this`. A recurring 12-instruction, handler-free single-call body accounts for 318 transitions; 312 of those old bodies bound its folded native target to an ordinary same-owner method. The remaining 60 old bodies contain an immediate-base call but have no typed-IL or behavioral acceptance. The folded callsite clears hidden MethodInfo, so receiver type and address alone cannot establish original managed callee identity. These methods remain failed; native shape counts are not recovered-constructor counts.

A paired player-only audit of the committed static `Int32` addition proof preserves all 38,700 and 91,198 ordered full-input method identities and the 7,256 and 12,724 selected denominators. Neither full input nor selected scope has a disposition transition from the preceding checkpoint, and the new proof matches no method in either full input. Selected totals remain 1,693 emitted / 4,544 failed / 2 partial / 1,017 without managed bodies and 4,204 emitted / 7,961 failed / 80 partial / 479 without managed bodies. Both broad strict scopes still reject unresolved behavior; neither has whole-scope typed IL, Unity compilation, native rebuild or behavioral-equivalence evidence from this audit. The single-method exact-target round trip is the accepted slice.

The paired audit of the committed object-field addition proof again preserves both ordered full-input identity sets and selected denominators. It finds no full-input or selected disposition transition and no method emitted by the new proof in either corpus. Selected counts remain 1,693 / 4,544 / 2 / 1,017 and 4,204 / 7,961 / 80 / 479 in emitted / failed / partial / no-body order. Both broad strict scopes reject; neither has whole-scope typed IL, Unity compilation, native rebuild or behavioral-equivalence evidence. The three-method exact-target round trip is the bounded acceptance result.

A paired player-only audit of the committed second-argument Boolean branch proof preserves all 38,700 and 91,198 ordered full-input method identities and the 7,256 and 12,724 selected denominators. Both full and selected scopes have zero disposition transitions from the preceding audit. Selected counts remain 1,693 / 4,544 / 2 / 1,017 and 4,204 / 7,961 / 80 / 479 in emitted / failed / partial / no-body order. The reports do not record proof invocations, so the direct match count is unavailable. Both broad strict scopes still reject unresolved behavior and have no whole-scope typed IL, Unity compilation, native rebuild or behavioral result from this audit.

A paired player-only audit after the static Boolean getter change preserves the same ordered full-input identities and selected denominators. The added runtime-helper and output-option gates reclassify 40 and 16 full-input methods from emitted to failed, including 40 and six in the selected scopes; no other disposition changes occur. Every reclassified method was previously emitted by the TypeInfo-guarded static getter proof and now reports unsupported native metadata initialization. Selected counts are 1,653 emitted / 4,584 failed / two partial / 1,017 without managed bodies out of 7,256, and 4,198 emitted / 7,967 failed / 80 partial / 479 without managed bodies out of 12,724. Those earlier emissions have no broad typed-IL, Unity compilation, native rebuild or behavioral acceptance and are not retained as verified recovery. Both broad strict scopes still reject unresolved behavior. The exact-target four-method fixture above is the accepted evidence for the added Boolean shape.

A paired player-only audit after the committed signed-Add and sequential-null changes retains all 38,700 and 91,198 ordered full-input identities and both selected denominators. The first corpus has no disposition transition. The second has four full-input failures become emitted after integer-local inference and two full-input emissions become failed under the native Add-width guard; all six are outside its selected assembly. Selected counts therefore remain 1,653 / 4,584 / 2 / 1,017 out of 7,256 and 4,198 / 7,967 / 80 / 479 out of 12,724 in emitted / failed / partial / no-body order. A separate diagnostic replay classifies 35 selected first failures at the new Add check as native 64-bit additions to destinations outside recognized 32/64-bit managed integer stack types; no guard was relaxed. Both broad strict scopes still fail. These disposition changes have no whole-scope typed-IL, Unity compilation, native rebuild or behavioral acceptance; the exact three-method fixture above is the bounded accepted result.

A paired player-only audit of the committed value-type receiver safety check plus the bounded reference-null proof retained all 38,700 and 91,198 full-input identities and both selected denominators. The selected scopes were 1,730 emitted / 4,507 failed / 2 partial / 1,017 without managed bodies and 4,398 emitted / 7,766 failed / 81 partial / 479 without managed bodies. Against the paired pre-reference-null snapshot, the first corpus had no disposition transition; the second gained thirty full-input emitted methods, including four selected, with no reverse transition. Earlier isolated XMM audits showed 1,838 and 4,414 selected emitted methods; a subsequently committed value-type receiver check reclassified 108 and 20 as failed because managed storage was unproved. These are analysis dispositions, not verified behaviors. Both broad strict runs still fail, and neither complete scope has typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence. The paired audit predates the terminal bounds diagnostic.

A fresh paired player-only audit after the bounded class-cast, literal, type-token, composed-call and terminal-throw proofs preserves all 38,700 and 91,198 ordered full-input identities and the 7,256 and 12,724 selected denominators. Both complete reports are byte-identical to the preceding audit, with zero full-input or selected disposition transitions. Selected counts remain 1,653 / 4,584 / 2 / 1,017 and 4,198 / 7,967 / 80 / 479 in emitted / failed / partial / no-body order. Both strict commands exit 1 with unresolved methods; neither broad scope has typed-IL, Unity compilation, native rebuilding or behavioral-equivalence acceptance. The exact-target fixtures above establish only their finite bounded paths, not progress through either broad selected denominator.

The two broad player inputs are authenticated as Unity 2021.3.35f1 Windows x64 IL2CPP, but no contemporaneous build receipt proves their IL2CPP compiler configuration was Release. A read-only probe of the 17-instruction terminal-throw cohort finds one of 26 first-corpus candidates rejected for an unproved value-type receiver and the other 25, plus all 293 second-corpus candidates, rejected by the alternate metadata initializer before MethodDef routing can be established. The initializer has a different native register/branch layout, so widening the current byte-length guard is unsound. Treat these cohorts as secondary observed player variants until an exact supplied-editor Release control reproduces and validates that helper.

The paired player-only audit after the unwind-bounded terminal-throw change preserves both ordered full-input identity sets and selected denominators. Neither corpus has a full-input or selected disposition or reason change. Selected counts remain 1,653 emitted / 4,584 failed / 2 partial / 1,017 without managed bodies out of 7,256, and 4,198 emitted / 7,967 failed / 80 partial / 479 without managed bodies out of 12,724. Both broad strict commands exit 1. No whole-scope typed IL, Unity compilation, native rebuild or behavioral-equivalence result is established, and Release configuration for these players remains unverified.

A paired player-only audit after the closed scalar-float reference-mutation proof retains all 38,700 and 91,198 ordered full-input methods and both selected denominators. Neither corpus has a disposition transition; the new proof emits no method in either broad input. Selected counts remain 1,653 / 4,584 / 2 / 1,017 out of 7,256 and 4,198 / 7,967 / 80 / 479 out of 12,724 in emitted / failed / partial / no-body order. Reasons change for 134 and 1,317 already-failed full-input methods, respectively, because the preceding ambiguous-call diagnostic now reports their native callsite evidence before provisional local typing. Both broad strict commands exit 1 before source emission, so no source-diagnostic or whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result is available. Their Release configuration remains unverified.

The authenticated paired audit after the initial forwarded-struct proof preserved all 38,700 and 91,198 ordered full-input method identities and the 7,256 and 12,724 selected denominators, with no disposition transition. Five selected independent-corpus candidates shared its eight-instruction caller and by-value two-float argument shape but had a static constructor and static fields, which the first proof deliberately excluded. The separate exact-target static control above established their initialization boundary before the proof was extended.

A later authenticated paired audit of the closed static-constructor and expanded forwarding proofs again preserves both full-input identity sets and selected denominators. The first corpus has no disposition transition and remains 1,653 emitted / 4,584 failed / two partial / 1,017 without managed bodies. Five selected methods in the second corpus move from failed to emitted with no reverse transition, bringing it to 4,203 emitted / 7,962 failed / 80 partial / 479 without managed bodies. Both broad strict scopes still reject unresolved behavior; neither has whole-scope typed IL, Unity compilation, native rebuilding or behavioral-equivalence acceptance, and their original Release configuration remains unverified. The six-method exact-target native round trip is the bounded acceptance evidence.

The next authenticated paired audit of the Boolean-getter metadata and generic-base constructor proofs keeps all 38,700 and 91,198 full-input method identities in order and both selected denominators unchanged. Three first-corpus methods outside its selected assembly move from failed to emitted; its selected counts remain 1,653 / 4,584 / two / 1,017. The second corpus has 23 full-input failed-to-emitted transitions, including 21 selected Boolean-return getters with no reverse transition. Its selected counts become 4,224 emitted / 7,941 failed / 80 partial / 479 without managed bodies out of 12,724. Both strict commands still exit 1 with unresolved behavior; neither broad scope has whole-scope typed IL, Unity compilation, native rebuilding or behavioral-equivalence acceptance. Their original Release configuration remains unverified. The separate eleven-method exact-target Boolean control passes the full native round trip above; that finite result does not validate these broad transitions.

An isolated original build of the independent application source with the supplied Windows editor selected Win64, IL2CPP, native `Release`, nondevelopment and OptimizeSpeed. Unity returned a successful build result and fresh native files, but its build report recorded three errors during scene processing, serialized-field packaging and a postbuild callback. The strict control therefore failed despite zero C# compiler and native linker errors. This input is retained for diagnostics only and does not validate the earlier independent player input's Release settings, whole-project compilation or behavioral equivalence.

In a repaired diagnostic clone, the postbuild exception is absent but two BuildReport errors remain. They repeat one Unity serialization field-name collision at scene building and asset packaging; the same diagnostic occurs in the earlier original-scene report, and the implicated application source was unchanged. The clone also lacks some plug-in metadata files and changes asset/settings state, so it does not establish a pristine-project build. The zero-error gate remains failed despite successful C# and native compilation. Future private controls should retain per-step BuildReport diagnostics under ignored `Files/`; this input-level serialization issue does not justify weakening recovery validation.

A further assetless original-source control preserved the application source, assembly definitions, paired importer metadata, required plug-ins, package manifest/lock and target settings while building one generated empty scene. The exact Windows editor compiled 28 assemblies, including the selected application assembly, and produced fresh x64 IL2CPP native files with no C# compiler errors. Its BuildReport still contains one instance of the same original duplicate serialized-field diagnostic. Excluding original scenes and noncode assets removed the separate packaging occurrence but did not clear the type/scene serialization error. The strict original native-build gate therefore remains failed, and this code-only player is diagnostic input rather than a clean Release acceptance baseline. Source, managed oracle, player inputs and raw receipts remain isolated under ignored `Files/`.

A paired player-only audit of the bounded folded-`Int32` constructor proof on that separate diagnostic input retains all 91,051 full and 12,722 selected method identities. Exactly 141 full methods, including 122 selected, move from Failed to Emitted; all are constructors, and no disposition regresses or changes otherwise. The selected scope moves from 4,823 Emitted / 7,340 Failed / 80 Partial / 479 without managed bodies to 4,945 / 7,218 / 80 / 479. The 122 selected gains previously failed at ambiguous native call identity and now pass label and stack-depth checks, but the broad strict run still exits 1 and no whole-scope typed IL, clean Unity build, native rebuild or behavioral-equivalence result exists. This diagnostic input is distinct from the earlier independent-player denominator; its original build has the serialization errors above and supplies no broad behavioral acceptance.

The direct-constructor iterator proof was then audited against the same diagnostic input. The first paired run correctly produced no gains because its raw-byte gate incorrectly required the method-size estimate to include the final trap; all 36 measured native templates estimate exactly 108 real bytes while their unwind regions include a 109th trap byte. The corrected gate independently authenticates both boundaries. A fresh paired audit preserved all 91,051 full and 12,722 selected ordered identities. Exactly 31 selected methods moved from Failed to Emitted, with no other transitions; the other five measured template candidates have parameters outside this zero-argument proof, and one is also virtual. Selected totals are now 4,976 Emitted / 7,187 Failed / 80 Partial / 479 without managed bodies. Strict recovery still exits 1, and this diagnostic player has no accepted clean original build, whole-scope typed IL, recovered Unity compilation, native rebuild or behavioral equivalence. These 31 dispositions are not a broad recovery acceptance result.

A paired player-only audit after the static `Int32` setter proof again preserves all 91,051 full and 12,722 selected ordered identities. It records no disposition transition in either scope; selected totals remain 4,976 Emitted / 7,187 Failed / 80 Partial / 479 without managed bodies, and strict recovery still exits 1. The single-method exact-target round trip supplies the bounded setter acceptance result; this broad audit adds no compilation, rebuild or behavior evidence.

| Recovery checkpoint | Corpus | Emitted | Failed | Partial | No managed body |
| --- | --- | ---: | ---: | ---: | ---: |
| Initial baseline | Private | 1,832 | 4,393 | 14 | 1,017 |
| Initial baseline | Independent | 4,927 | 7,214 | 104 | 479 |
| Width, flag and ABI checks | Private | 1,874 | 4,357 | 8 | 1,017 |
| Width, flag and ABI checks | Independent | 4,847 | 7,299 | 99 | 479 |
| Initialization/exception preservation; floating and byte-field support | Private | 1,672 | 4,561 | 6 | 1,017 |
| Initialization/exception preservation; floating and byte-field support | Independent | 4,415 | 7,655 | 175 | 479 |
| Integer-extension and native-boundary proofs | Private | 1,664 | 4,572 | 3 | 1,017 |
| Integer-extension and native-boundary proofs | Independent | 4,210 | 7,955 | 80 | 479 |
| Null-check and SIMD safety proofs | Private | 1,696 | 4,542 | 1 | 1,017 |
| Null-check and SIMD safety proofs | Independent | 3,717 | 8,448 | 80 | 479 |
| Zero-adjustment near-return proof | Private | 1,734 | 4,504 | 1 | 1,017 |
| Zero-adjustment near-return proof | Independent | 4,293 | 7,872 | 80 | 479 |
| Bounded `int[]` read proof | Private | 1,734 | 4,504 | 1 | 1,017 |
| Bounded `int[]` read proof | Independent | 4,293 | 7,872 | 80 | 479 |
| Bounded signed `int` instance-field null guard | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded signed `int` instance-field null guard | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded signed `int` instance-field store | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded signed `int` instance-field store | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded `int[]` element store and terminal bounds exit | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded `int[]` element store and terminal bounds exit | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded signed 64-bit instance-field access | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded signed 64-bit instance-field access | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded Boolean zero store | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded Boolean zero store | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded nested signed 32-bit zero store | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded nested signed 32-bit zero store | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded direct signed 32-bit zero store | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded direct signed 32-bit zero store | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded Boolean field read | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded Boolean field read | Independent | 4,300 | 7,864 | 81 | 479 |
| Bounded second-argument Boolean field read | Private | 1,734 | 4,503 | 2 | 1,017 |
| Bounded second-argument Boolean field read | Independent | 4,300 | 7,864 | 81 | 479 |
| Direct tail-call boundary and bounded array-returning call | Private | 1,742 | 4,495 | 2 | 1,017 |
| Direct tail-call boundary and bounded array-returning call | Independent | 4,319 | 7,845 | 81 | 479 |
| Bounded guarded string-field reads | Private | 1,742 | 4,495 | 2 | 1,017 |
| Bounded guarded string-field reads | Independent | 4,319 | 7,845 | 81 | 479 |
| Derived reference reads and shared constructor chain | Private | 1,742 | 4,495 | 2 | 1,017 |
| Derived reference reads and shared constructor chain | Independent | 4,319 | 7,845 | 81 | 479 |
| Unchanged array-neighbor field layout | Private | 1,758 | 4,479 | 2 | 1,017 |
| Unchanged array-neighbor field layout | Independent | 4,349 | 7,815 | 81 | 479 |
| Bounded shared-address string reads | Private | 1,758 | 4,479 | 2 | 1,017 |
| Bounded shared-address string reads | Independent | 4,349 | 7,815 | 81 | 479 |
| Bounded object-field reads | Private | 1,758 | 4,479 | 2 | 1,017 |
| Bounded object-field reads | Independent | 4,349 | 7,815 | 81 | 479 |
| Bounded `int[]` field reads | Private | 1,758 | 4,479 | 2 | 1,017 |
| Bounded `int[]` field reads | Independent | 4,349 | 7,815 | 81 | 479 |
| Bounded reference reads from `this` | Private | 1,758 | 4,479 | 2 | 1,017 |
| Bounded reference reads from `this` | Independent | 4,349 | 7,815 | 81 | 479 |
| Bounded application-class field reads | Private | 1,760 | 4,477 | 2 | 1,017 |
| Bounded application-class field reads | Independent | 4,351 | 7,813 | 81 | 479 |
| Bounded reference-element array field reads | Private | 1,760 | 4,477 | 2 | 1,017 |
| Bounded reference-element array field reads | Independent | 4,351 | 7,813 | 81 | 479 |
| Direct handler-free chained unwind | Private | 1,761 | 4,476 | 2 | 1,017 |
| Direct handler-free chained unwind | Independent | 4,351 | 7,813 | 81 | 479 |
| Bounded unsigned 32-bit array access | Private | 1,761 | 4,476 | 2 | 1,017 |
| Bounded unsigned 32-bit array access | Independent | 4,351 | 7,813 | 81 | 479 |
| Bounded signed 64-bit array access | Private | 1,761 | 4,476 | 2 | 1,017 |
| Bounded signed 64-bit array access | Independent | 4,351 | 7,813 | 81 | 479 |
| Bounded unsigned 64-bit array access | Private | 1,761 | 4,476 | 2 | 1,017 |
| Bounded unsigned 64-bit array access | Independent | 4,351 | 7,813 | 81 | 479 |
| Unchanged derived receiver and reachable constructor thunk | Private | 1,761 | 4,476 | 2 | 1,017 |
| Unchanged derived receiver and reachable constructor thunk | Independent | 4,365 | 7,799 | 81 | 479 |
| Shared terminal runtime null helper | Private | 1,791 | 4,446 | 2 | 1,017 |
| Shared terminal runtime null helper | Independent | 4,380 | 7,784 | 81 | 479 |

The integer-extension checkpoint authenticated prior inputs and preserved every selected and full-input method identity. Ten private and 300 independent methods previously marked emitted failed on unproved integer extensions. Two private and 95 independent partial methods became emitted solely because a native proof resolved their unused-receiver mapping warning. One additional private partial method failed on an unproved extension. The fallthrough boundary check became the first diagnostic for 490 private and 1,264 independent methods that already failed.

The current corpus refresh again authenticates the input and complete method denominators. Relative to the preceding null-check/SIMD checkpoint, the zero-adjustment return proof moves 38 private and 576 independent methods from failed to emitted, with no emitted-to-failed transition. Both full application scopes still fail strict recovery; neither has a typed-IL, Unity compilation, native rebuild or behavioral equivalence result. First-diagnostic counts are not root-cause counts, and disposition changes do not establish behavioral accuracy.

A subsequent authenticated player-only refresh after the bounded array-read proof preserves all 7,256 private and 12,724 independent selected identities and dispositions, with no transition in either direction. The new proof therefore adds only its controlled synthetic acceptance result at this checkpoint. Both broad scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after the bounded instance-field proof again preserves every selected and full-input method identity. In the private scope, one method changes from failed to partial and none becomes emitted. In the independent scope, seven change from failed to emitted and one from failed to partial; no emitted method regresses. Both complete scopes still fail strict recovery. These are analysis dispositions only: the broad corpora have no typed-IL verification, Unity compilation, native rebuild or behavioral equivalence result at this checkpoint.

An authenticated refresh after the parameter-supplied instance-field store proof preserves the same complete denominators and every selected disposition in both corpora. This extends only the controlled synthetic acceptance scope at this checkpoint. Both broad scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after the bounded `int[]` store and terminal-helper boundary proof again preserves both complete denominators and every selected disposition. The controlled read/write fixture passes, while both broad scopes still fail strict recovery. Neither broad scope has typed-IL, Unity compilation, native rebuild or behavioral equivalence evidence from this refresh.

The authenticated refresh after signed 64-bit field-access support preserves both complete denominators and every selected disposition again. The expanded controlled fixture passes all exact-target gates; both broad scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral equivalence result.

An exact runtime-metadata-guard diagnostic pass preserves all 7,256 private and 12,724 independent selected identities and dispositions. It identifies the first unresolved reason for 901 private and 90 independent methods as a metadata-helper initialization guard with a matching byte flag and branch. Native inspection confirmed that a representative independent guard feeds static-field access and a tail transfer. This classification does not remove the helper or flag write, recover class-initialization effects, or establish source compilation or behavioral equivalence. Both complete scopes still fail strict recovery.

The authenticated refresh after the bounded Boolean zero-store proof preserves both full input identity sets and all selected dispositions: 7,256 private and 12,724 independent methods. This proof adds only the exact-target synthetic acceptance result at this checkpoint. Neither broad scope has typed-IL, Unity compilation, native rebuild or behavioral equivalence evidence, and both still fail strict recovery.

The authenticated refresh after the nested signed 32-bit zero-store proof again preserves both full input identity sets and every selected disposition. The exact-target synthetic fixture passes strict player-only recovery, typed IL, both declaration gates, source compilation, Release native rebuilding and the tested behavior. Neither broad scope has those validation gates, and both still fail strict recovery.

The authenticated refresh after the direct signed 32-bit zero-store proof preserves both full input identity sets and every selected disposition again. This checkpoint adds one verified method in the controlled exact-target fixture. Both complete application scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after the bounded Boolean field-read proof preserves both selected application denominators and dispositions. One method outside the selected independent application assembly changes from failed to emitted in the full input report; it has no typed-IL, Unity compilation, native rebuild or behavioral equivalence result. Both complete application scopes still fail strict recovery.

The authenticated refresh after the bounded second-argument Boolean read and unused-receiver proof preserves both full input identity sets, both selected application denominators and all dispositions relative to the prior checkpoint. The exact-target fixture passes every declared gate, but neither broad application scope has typed-IL, Unity compilation, native rebuild or behavioral equivalence evidence and both still fail strict recovery.

An authenticated refresh after the direct-tail boundary and bounded array-call changes preserves both full input identity sets and selected denominators. Eight private and 30 independent full-input methods move from failed to emitted, including 19 in the selected independent assembly; no emitted method regresses. These are analysis dispositions only. Both complete scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after the bounded string-field read proof preserves both full input identity sets, both selected application denominators and every disposition. The exact-target fixture passes all declared gates; the broad corpora still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after derived-owner reads and the shared-constructor-chain repair again preserves both full input identity sets, selected denominators and every disposition. It measures no broad coverage gain; the six-method exact-target fixture passes all declared gates. Neither complete application scope has typed-IL, Unity compilation, native rebuild or behavioral equivalence evidence from this refresh, and both still fail strict recovery.

The authenticated refresh after the array-wrapper layout correction preserves every full-input method identity and both selected denominators. Sixteen selected private and thirty selected independent methods move from failed to emitted; across the full inputs the transitions are sixteen and fifty-three respectively, with no reverse transition. Both complete application scopes still fail strict recovery. These disposition changes have no broad typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence; only the seventeen-observation controlled fixture passes those gates.

The authenticated refresh after admitting only method-specific, fully proved shared-address string reads preserves both full-input identity sets, selected denominators and every disposition. The ten-method controlled fixture passes all declared gates, but this change measures no additional broad recovery coverage. Both complete application scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence from this refresh.

The authenticated refresh after the bounded object-field read extension preserves both full-input identity sets, selected denominators and every disposition again. The twelve-method exact-target fixture passes all declared gates; neither broad application scope has typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence from this refresh, and both still fail strict recovery.

The authenticated refresh after the bounded `int[]` field-read extension again preserves both full-input identity sets, selected denominators and every disposition. The fourteen-method exact-target fixture passes all declared gates, but neither broad application scope has typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence from this refresh. Both still fail strict recovery.

The authenticated refresh after accepting exact direct reference-field reads from `this` preserves both full-input identity sets, selected denominators and every disposition again. The seventeen-method exact-target fixture passes all declared gates, including the fresh Windows Release IL2CPP rebuild and fifty-three behavioral observations at each editor/player stage. Both broad scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral-equivalence result from this refresh.

The authenticated refresh after bounded application-class field reads preserves both full-input identity sets and selected denominators. Four private and seven independent full-input methods move from failed to emitted, including two in each selected application assembly; none moves in the reverse direction. These are analysis dispositions only. Both complete scopes still fail strict recovery and have no typed-IL, Unity compilation, native rebuild or behavioral-equivalence result from this refresh.

The authenticated refresh after bounded `string[]` and `object[]` field reads preserves both full-input identity sets, selected denominators and every disposition. The twenty-five-method exact-target fixture passes all declared gates, but neither broad application scope has typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence from this refresh. Both still fail strict recovery.

The authenticated refresh after direct handler-free chained unwind preserves both complete input identity sets and selected denominators. One private method changes from failed to emitted; the independent application's dispositions do not change. Both broad scopes still fail strict recovery. The changed method has no source-compilation, native-rebuild or behavioral acceptance result, so the transition is a recovery-analysis result only.

The authenticated refresh after bounded unsigned 32-bit array access preserves both full-input identity sets, selected denominators and every disposition. The four-method exact-target fixture passes all declared recovery, declaration, compilation, rebuild and behavioral gates. Neither broad application scope passes strict recovery or has a source-compilation, native-rebuild or behavioral-equivalence result from this refresh.

The authenticated refresh after bounded signed 64-bit array access again preserves both complete input identity sets, selected denominators and every disposition. The six-method exact-target fixture passes all declared gates. Both broad application scopes still fail strict recovery and have no whole-scope Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after bounded unsigned 64-bit array access again preserves both complete input identity sets, selected denominators and every disposition. The eight-method exact-target fixture passes all declared gates. Both broad application scopes still fail strict recovery and have no whole-scope Unity compilation, native rebuild or behavioral equivalence result.

The authenticated refresh after the derived-receiver and reachable constructor-thunk proofs preserves both full-input identity sets and selected denominators. Across the full inputs, one private and nineteen independent methods move from failed to emitted with no reverse transition; fourteen of the independent transitions are in its selected application assembly, and none of the private transitions is in its selected assembly. The first diagnostic is still an uncoalesced runtime null guard for 1,549 selected private and 3,818 selected independent methods. These broad results are analysis dispositions only: both complete scopes still fail strict recovery, with no whole-scope typed IL, Unity compilation, native rebuild or behavioral result. The five-method synthetic fixture above is the bounded acceptance evidence.

The authenticated refresh after accepting a shared terminal null helper again preserves both complete input identity sets and selected denominators. Across the full inputs, forty-four private and nineteen independent methods move from failed to emitted, including thirty and fifteen respectively in the selected application assemblies; none moves in the reverse direction. The null guard remains the first diagnostic for 1,473 selected private and 3,728 selected independent methods. Both complete scopes still fail strict recovery, and these transitions have no broad typed-IL, Unity compilation, native rebuild or behavioral acceptance result. The six-method synthetic player above supplies the bounded acceptance evidence.

A paired authenticated audit after the reference-null, reference-store, byte-threshold and write-barrier range proofs retains all 38,700 and 91,198 ordered input method identities. The selected private scope remains 1,730 emitted, 4,507 failed, two partial and 1,017 without managed bodies out of 7,256. The selected independent scope remains 4,398 emitted, 7,766 failed, 81 partial and 479 without managed bodies out of 12,724. Both strict scopes still exit with failure and have no whole-scope typed IL, Unity compilation, rebuild or behavioral-equivalence result. Two methods outside the selected independent assembly differ from the immediately preceding report because two locals remain untyped. They also failed in 47 of 48 historical reports and already failed before the new native proofs; neither new proof matches their shapes. The single earlier emission is not accepted as recovery evidence, and its cause remains unresolved.

An authenticated paired audit after the one-byte array proof again retains all 38,700 and 91,198 ordered input identities and both selected denominators. Relative to the preceding paired snapshot, neither corpus has a full-input or selected disposition transition. The selected totals remain 1,730 emitted / 4,507 failed / two partial / 1,017 without managed bodies out of 7,256, and 4,398 emitted / 7,766 failed / 81 partial / 479 without managed bodies out of 12,724. Both broad strict runs fail; neither has a whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result. The four-method synthetic round trip above is the bounded acceptance result.

An authenticated paired audit after the direct Single-array proof again retains all 38,700 and 91,198 ordered input identities and both selected denominators. Neither corpus has a full-input or selected disposition transition from the preceding paired snapshot. Selected totals remain 1,730 emitted / 4,507 failed / two partial / 1,017 without managed bodies out of 7,256, and 4,398 emitted / 7,766 failed / 81 partial / 479 without managed bodies out of 12,724. Both broad strict runs fail; neither has whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence evidence. The two-method controlled Single-array round trip above is the bounded acceptance result.

The authenticated paired audit after the signed/unsigned 16-bit read proof again retains the same 38,700 and 91,198 ordered input identities and selected denominators, with no full-input or selected disposition transitions. Selected totals remain 1,730 emitted / 4,507 failed / two partial / 1,017 without managed bodies out of 7,256, and 4,398 emitted / 7,766 failed / 81 partial / 479 without managed bodies out of 12,724. Both broad scopes still fail strict recovery, and no whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result is established. The controlled two-method round trip is the bounded acceptance result.

The authenticated paired audit after the 32-bit static-getter proof retains all 38,700 and 91,198 ordered input identities and the 7,256 and 12,724 selected denominators. Neither full input nor selected scope has a disposition transition from the preceding paired snapshot. Selected totals remain 1,730 emitted / 4,507 failed / two partial / 1,017 without managed bodies, and 4,398 emitted / 7,766 failed / 81 partial / 479 without managed bodies, respectively. Both broad scopes still fail strict recovery and have no whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result. The controlled two-method round trip above is the bounded acceptance result.

The historical authenticated paired audit after the field-backed `int[]` proof and shared output-option rejection retains the same 38,700 and 91,198 ordered input identities and both selected denominators. Neither full input nor selected scope has a disposition transition from the preceding snapshot. Selected totals remain 1,730 emitted / 4,507 failed / two partial / 1,017 without managed bodies out of 7,256, and 4,398 emitted / 7,766 failed / 81 partial / 479 without managed bodies out of 12,724. Both broad strict runs still reject unresolved methods; no whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result exists. The controlled four-method round trip above was a historical bounded acceptance result at that audit; a later bounded constructor proof and fresh exact-target round trip restore strict acceptance.

The authenticated paired audit after the bounded Boolean getter proof retains all 38,700 and 91,198 ordered input identities and both selected denominators. Fifty-eight and 368 full-input methods, respectively, move from failed to emitted with no reverse transition. The selected scopes move to 1,731 emitted / 4,506 failed / two partial / 1,017 without managed bodies out of 7,256, and 4,606 emitted / 7,558 failed / 81 partial / 479 without managed bodies out of 12,724. The new transitions all have ordinary zero-parameter Boolean-return signatures; audit reports alone do not prove their native semantics. Both complete scopes still fail strict recovery and lack whole-scope typed-IL verification, Unity compilation, native rebuilding and behavioral equivalence. The four-method exact-target fixture supplies the bounded acceptance result.

A separate fresh Release diagnostic player comes from the supplied Windows editor and has valid native binary and metadata, but its independent original project build reports three Unity project-integration errors; it is not a clean validation baseline. After the bounded cast-ancestor and direct-assembly-reference proof, a strict player-only audit retains all 91,051 input method identities and 12,722 selected identities. Seventy-five selected methods move from failed to emitted with no reverse transition; seventy are members of an inspected 125-method virtual string-getter cohort with the exact native caller/helper shapes. The other five are direct class-return getters. Each has a distinct single-bound native address, an unchanged getter-only property declaration, and a complete handler-free body passing the same closed cast proof. This authenticates their native proof matches but not their recovered behavior. Selected dispositions remain 4,823 emitted / 7,340 failed / 80 partial / 479 without managed bodies, and strict recovery exits with failure. The cohort's 52 private-inherited-field cases and three target-static-constructor cases remain rejected. These are diagnostic analysis dispositions only, with no broad typed-IL, source-compilation, native-rebuild or behavioral acceptance claim.

A separate isolated original-source, empty-scene control produced a fresh Windows x64 Release IL2CPP diagnostic player, but its Unity BuildReport retained two errors and the editor exited nonzero. The clone also lost 25 plug-in metadata files and changed one plug-in cache asset and one project-setting file; a private editor-only callback repair removed its observed exception without clearing the reported error count. The player is therefore not a clean original baseline or a recovered-code validation. Its selected application assembly retained 12,722 methods, including 12,315 with native bodies. Strict player-only recovery reports 4,748 emitted, 7,415 failed, 80 partial and 479 without managed bodies; the full diagnostic input has 91,039 methods. These counts are diagnostic dispositions only. The scoped generated project/player copies were pruned after retaining private receipts, logs and minimal hashed inputs.

A fresh paired player-only audit from the current tool snapshot reauthenticates both input sets against their retained receipts and preserves all 38,700 and 91,198 ordered method identities and the 7,256 and 12,724 selected denominators. Against the matched historical receipts, 32 and 312 full-input methods move from failed to emitted, including 25 and 288 in the selected scopes; no reverse transitions occur. Selected dispositions are 1,678 emitted / 4,559 failed / two partial / 1,017 without managed bodies and 4,512 emitted / 7,653 failed / 80 partial / 479 without managed bodies, respectively. All changed methods are one-`Int32`-argument constructors that match the closed folded-constructor proof; none uses the bounded nested Boolean-store proof. The earlier generic call path rejected their shared native target as ambiguous. The closed proof authenticates an inert native target and the caller field store before supplying the mandatory direct-`Object` base call, while the original folded callsite MethodDef identity remains unavailable. This attributes the disposition transitions to that proof but does not behaviorally validate the broad methods. Both complete scopes still fail strict recovery and have no whole-scope typed-IL, Unity compilation, native rebuild or behavioral-equivalence result.

- Run the user-supplied source/build pair locally as an independent validation case. Keep source/original assemblies inaccessible to the player-only recovery step, and use them afterwards for comparisons.
- Validate the private target with the same reporting, compilation and behavioral gates to the extent an oracle is available. Without source or an equivalent oracle, report the narrower observed evidence honestly.
- Convert general failures into minimal synthetic regressions. Never encode private game names, method names, offsets, hashes or input-specific exceptions into tracked recovery logic.
- Publish only sanitized aggregate findings: explicit corpus scope, feature coverage, compile/build results, behavioral counts, exclusions and remaining defects. Set measurable coverage targets from the baseline before making a 1:1 accuracy claim.
- Deduplicate proven common parsing/ABI/type/emission logic, simplify stale paths, and profile costly analysis. Preserve diagnostics and regression evidence during optimization.
- Make public synthetic tests practical for this fork's CI without private files, licenses or publishing credentials. Keep licensed/editor-dependent runs optional and explicitly labeled; CI setup does not authorize pushing or publishing.

Exit evidence: increasing coverage on controlled and independent inputs, explainable remaining gaps, repeatable results and no private information in tracked changes. A compiling project alone does not satisfy this milestone's accuracy goal.

## Checkpoints and completion claims

After each coherent implementation change, run checks appropriate to its risk, review the staged diff for private information, make a local checkpoint commit, and update the relevant milestone with a short sanitized evidence summary. Keep raw logs and long investigation trails out of this roadmap. Only the user pushes.

Support claims must state the exact tested profile and corpus, declaration fidelity, source compilation and native-build results, behavioral coverage, and unresolved counts. The prior 165 methods across 30 player-only fixtures plus three direct reference-array reads yield 168 methods across 31 fixtures with player-only recovery and exact native behavioral receipts. The bounded Boolean-getter and framework-reference fixtures add five player-only methods, for 173 methods across 33 fixtures. The external-reference fixture adds one method and one fixture with an explicit map and synthetic managed dependency assemblies, for 174 methods across 34 fixtures. The seven-method manual iterator-factory and seven-method literal-concatenation fixtures add two exact native round trips, for 188 methods across 36 fixtures. The TypeInfo-guarded static addition fixture adds one, for 189 methods across 37 fixtures. The separate three-method moved-guard fixture adds three fixture methods, for 192 across 38 fixtures. The three-method second-argument Boolean branch control brings the total to 195 across 39 fixtures. One added method in the established static-getter fixture brings the total to 196 fixture methods across 39 fixtures with verified recovered Unity projects and native behavioral receipts. Declaration zero-difference results in this aggregate are comparisons against fixture managed oracles; version-29 player-only return rows and return attributes remain explicitly unknown. This aggregate counts fixture methods, including a separate control of the static-addition method, not a percentage of either broad application. There is currently no measured 1:1 accuracy result for a broader corpus or either private game. The sequential-null-guard fixture adds three methods and one native receipt, bringing the historical total to 199 methods across 40 fixtures. The later cast-only, static-literal, combined cast/literal, terminal-throw and second handwritten iterator controls add 29 fixture methods across five exact native round trips, for a historical total of 228 across 45 fixtures. The large-offset nested Boolean-store control adds three more methods and one exact native round trip, for a historical total of 231 across 46 fixtures. The three-field scalar-float reference-mutation control adds two methods and one exact native round trip, for a historical total of 233 across 47 fixtures. The compiler-generated iterator control adds eight selected methods and one exact native round trip, bringing that historical total to 241 methods across 48 fixtures; its declaration projection matches a managed oracle, while `DECL001` and `DECL004` remain unresolved from player inputs alone. The four-method ordinary forwarded-argument validation control and five-method bounded by-value struct proof add two exact native round trips, bringing the historical total to 250 methods across 50 fixtures. The ordinary control is an existing recovery path, and the initial struct proof measured no broad disposition gain. The six-method static-struct control adds one complete native round trip, bringing the historical total to 256 methods across 51 fixtures; its later proof expansion moves five selected broad methods to emitted without whole-scope behavioral validation. The eleven-method Boolean-getter metadata control adds one complete native round trip, bringing the historical total to 267 methods across 52 fixtures. The five-method auxiliary-assisted virtual string getter and guarded-constructor control brings the historical total to 272 methods across 53 fixtures with exact native receipts. The field-backed array fixture again passes current strict recovery and exact native round-trip gates after the bounded constructor proof; this restores one already counted fixture and does not increase the 272-method, 53-fixture total. The generic-dispatch, guarded-sink and folded-constructor controls add nine selected identities across three complete native round trips, bringing the bounded fixture scope to 281 methods across 56 fixtures. Eight of those nine methods have emitted bodies; the ninth is an abstract interface method correctly classified as having no managed body. The direct-constructor iterator and static `Int32` setter controls add nine emitted methods across two complete native round trips, bringing the scope to 290 selected methods across 58 fixtures. The four-method instance-reference-setter fixture adds one complete native round trip, bringing the bounded scope to 294 selected methods across 59 fixtures. The four-method unused-reference nested-store fixture adds one more complete native round trip, bringing the scope to 298 selected methods across 60 fixtures. The five-method read/write reference-property fixture adds another complete native round trip, bringing the bounded scope to 303 selected methods across 61 fixtures. This aggregate remains a collection of bounded controls rather than broad recovery coverage.
